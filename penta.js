/* eslint-disable */ 
(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define("Penta", [], factory);
	else if(typeof exports === 'object')
		exports["Penta"] = factory();
	else
		root["Penta"] = factory();
})(this, function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./index.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./index.js":
/*!******************!*\
  !*** ./index.js ***!
  \******************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\n/* eslint-disable */\nvar Penta = __webpack_require__(/*! ./lib/penta */ \"./lib/penta.js\");\n\nmodule.exports = Penta;\n\n//# sourceURL=webpack://Penta/./index.js?");

/***/ }),

/***/ "./lib/config.js":
/*!***********************!*\
  !*** ./lib/config.js ***!
  \***********************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nmodule.exports = {\n  defaultBlock: \"latest\",\n  POLLING_TIMEOUT: 3000\n};\n\n//# sourceURL=webpack://Penta/./lib/config.js?");

/***/ }),

/***/ "./lib/httpRequest.js":
/*!****************************!*\
  !*** ./lib/httpRequest.js ***!
  \****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * http request\n * @class HttpRequest\n * @author Buddywangw <buddywangw@gmail.com>\n */\n\nvar Axios = __webpack_require__(/*! axios */ \"./node_modules/axios/index.js\");\nvar utils = __webpack_require__(/*! ./utils */ \"./lib/utils/index.js\");\n\nAxios.defaults.headers.post[\"Content-Type\"] = \"application/json\";\n\nvar HttpRequest = function () {\n  function HttpRequest(host) {\n    _classCallCheck(this, HttpRequest);\n\n    this.host = host || \"http://localhost:18545\";\n    this.axios = Axios.create();\n  }\n\n  /**\n   * send request\n   * @param {object} payload - request param\n   */\n\n\n  _createClass(HttpRequest, [{\n    key: \"send\",\n    value: function send(payload) {\n      var sendParams = {\n        method: \"post\",\n        url: this.host,\n        data: payload\n      };\n\n      return this.axios(sendParams).then(function (resp) {\n        var data = resp.data;\n        if (utils.isArray(data)) {\n          return data;\n        }\n        if (data.error) {\n          throw new Error(data.error.message);\n        }\n        return data.result;\n      }).catch(function (e) {\n        throw new Error(e.message);\n      });\n    }\n  }, {\n    key: \"sendAsync\",\n    value: function sendAsync(payload, callback) {\n      return this.send(payload).then(function (data) {\n        callback(data);\n      }).catch(function (err) {\n        callback(err);\n      });\n    }\n  }, {\n    key: \"isConnected\",\n    value: function isConnected() {\n      var sendParams = {\n        method: \"post\",\n        url: this.host,\n        data: {\n          id: 9999999999,\n          jsonrpc: \"2.0\",\n          method: \"\",\n          params: []\n        }\n      };\n\n      return this.axios(sendParams).then(function () {\n        return true;\n      }).catch(function () {\n        return false;\n      });\n    }\n  }]);\n\n  return HttpRequest;\n}();\n\nmodule.exports = HttpRequest;\n\n//# sourceURL=webpack://Penta/./lib/httpRequest.js?");

/***/ }),

/***/ "./lib/ipcRequest.js":
/*!***************************!*\
  !*** ./lib/ipcRequest.js ***!
  \***************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * ipc request\n * @class IpcRequest\n * @author Buddywangw <buddywangw@gmail.com>\n */\n\nvar utils = __webpack_require__(/*! ./utils */ \"./lib/utils/index.js\");\n\nvar IpcRequest = function () {\n  function IpcRequest(path, net) {\n    _classCallCheck(this, IpcRequest);\n\n    this.path = path;\n    this.responseCallbacks = {};\n    this.socket = net.connect({ path: this.path });\n\n    this._bindEvent();\n  }\n\n  _createClass(IpcRequest, [{\n    key: \"_bindEvent\",\n    value: function _bindEvent() {\n      var _this = this;\n\n      this.socket.on(\"error\", function (err) {\n        console.log(\"soket error\", err);\n      });\n\n      this.socket.on(\"connect\", function () {\n        console.log(\"socket connected\");\n      });\n\n      this.socket.once(\"connect\", function () {\n        console.log(\"socket once connected\");\n      });\n\n      this.socket.on(\"data\", function (data) {\n        _this._parseResponse(data).forEach(function (result) {\n          var id = null;\n          if (utils.isArray(result)) {\n            result.forEach(function (load) {\n              if (this.responseCallbacks[load.id]) id = load.id;\n            });\n          } else {\n            id = result.id;\n          }\n          if (_this.responseCallbacks[id]) {\n            _this.responseCallbacks[id].resolve(result.result);\n            delete _this.responseCallbacks[id];\n          }\n        });\n      });\n    }\n\n    /**\n     * 响应结果格式化\n     * @param {string} data 响应结果\n     */\n\n  }, {\n    key: \"_parseResponse\",\n    value: function _parseResponse(data) {\n      var returnValues = [];\n      // console.log(\"data\", data.toString(\"utf-8\"));\n      var formatData = data.replace(/\\}[\\n\\r]?\\{/g, \"}|--|{\") // }{\n      .replace(/\\}\\][\\n\\r]?\\[\\{/g, \"}]|--|[{\") // }][{\n      .replace(/\\}[\\n\\r]?\\[\\{/g, \"}|--|[{\") // }[{\n      .replace(/\\}\\][\\n\\r]?\\{/g, \"}]|--|{\") // }]{\n      .split(\"|--|\");\n\n      formatData.forEach(function (data) {\n        var result = null;\n        try {\n          result = JSON.parse(data);\n        } catch (e) {\n          console.error(\"result parse\", e);\n          return;\n        }\n        if (result) returnValues.push(result);\n      });\n\n      return returnValues;\n    }\n\n    /**\n     * send request\n     * @param {object} payload - request param\n     */\n\n  }, {\n    key: \"send\",\n    value: function send(payload) {\n      var _this2 = this;\n\n      if (!this.socket.writable) this.socket.connect({ path: this.path });\n      this.socket.write(JSON.stringify(payload));\n      return new Promise(function (resolve, reject) {\n        _this2.responseCallbacks[payload.id] = { resolve: resolve, reject: reject };\n      });\n    }\n  }, {\n    key: \"isConnected\",\n    value: function isConnected() {\n      if (!this.socket.writable) this.socket.connect({ path: this.path });\n      return !!this.socket.writable;\n    }\n  }]);\n\n  return IpcRequest;\n}();\n\nmodule.exports = IpcRequest;\n\n//# sourceURL=webpack://Penta/./lib/ipcRequest.js?");

/***/ }),

/***/ "./lib/jsonrpc.js":
/*!************************!*\
  !*** ./lib/jsonrpc.js ***!
  \************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nmodule.exports = {\n  messageId: 0,\n  toPayload: function toPayload(method, params) {\n    if (!method) console.error(\"jsonrpc method is required!\");\n    this.messageId++;\n    return {\n      jsonrpc: \"2.0\",\n      id: this.messageId,\n      method: method,\n      params: params || []\n    };\n  }\n};\n\n//# sourceURL=webpack://Penta/./lib/jsonrpc.js?");

/***/ }),

/***/ "./lib/penta.js":
/*!**********************!*\
  !*** ./lib/penta.js ***!
  \**********************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * Penta entry\n */\nvar HttpRequest = __webpack_require__(/*! ./httpRequest */ \"./lib/httpRequest.js\");\nvar IpcRequest = __webpack_require__(/*! ./ipcRequest */ \"./lib/ipcRequest.js\");\nvar WebsocketRequest = __webpack_require__(/*! ./websocketRequest */ \"./lib/websocketRequest.js\");\nvar PollingManager = __webpack_require__(/*! ./pollingManager */ \"./lib/pollingManager.js\");\n\nvar Account = __webpack_require__(/*! ./penta/account */ \"./lib/penta/account.js\");\nvar Admin = __webpack_require__(/*! ./penta/admin */ \"./lib/penta/admin.js\");\nvar Blockchain = __webpack_require__(/*! ./penta/blockchain */ \"./lib/penta/blockchain.js\");\nvar Net = __webpack_require__(/*! ./penta/net */ \"./lib/penta/net.js\");\nvar Protocol = __webpack_require__(/*! ./penta/protocol */ \"./lib/penta/protocol.js\");\nvar Tx = __webpack_require__(/*! ./penta/tx */ \"./lib/penta/tx.js\");\nvar Txpool = __webpack_require__(/*! ./penta/txpool */ \"./lib/penta/txpool.js\");\nvar Filter = __webpack_require__(/*! ./penta/filter */ \"./lib/penta/filter.js\");\nvar Miner = __webpack_require__(/*! ./penta/miner */ \"./lib/penta/miner.js\");\nvar version = __webpack_require__(/*! ./version.json */ \"./lib/version.json\");\nvar Jsonrpc = __webpack_require__(/*! ./jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar utils = __webpack_require__(/*! ./utils */ \"./lib/utils/index.js\");\n\nvar Penta = function () {\n  function Penta() {\n    _classCallCheck(this, Penta);\n\n    this.sockets = [];\n    this.HttpRequest = HttpRequest;\n    this.IpcRequest = IpcRequest;\n    this.WebsocketRequest = WebsocketRequest;\n    this.version = {\n      api: version.version\n    };\n  }\n\n  _createClass(Penta, [{\n    key: \"setWebSocket\",\n    value: function setWebSocket(websocketUrl) {\n      this.websocketUrl = websocketUrl;\n    }\n  }, {\n    key: \"webSocketWatch\",\n    value: function webSocketWatch(params, callback) {\n      var socket = new WebsocketRequest(this.websocketUrl, function () {\n        socket.watch(params, callback);\n      });\n      this.sockets.push(socket);\n      return socket;\n    }\n  }, {\n    key: \"setRequest\",\n    value: function setRequest(request) {\n      var _this = this;\n\n      this._request = request;\n      this.pollingManager = new PollingManager(this);\n      this.account = new Account(this);\n      this.admin = new Admin(this);\n      this.blockchain = new Blockchain(this);\n      this.net = new Net(this);\n      this.protocol = new Protocol(this);\n      this.tx = new Tx(this);\n      this.txpool = new Txpool(this);\n      this.filter = function (options, callback) {\n        return new Filter(_this, options, callback);\n      };\n      this.miner = new Miner(this);\n    }\n  }, {\n    key: \"reset\",\n    value: function reset() {\n      this.sockets.forEach(function (item) {\n        item.stop();\n      });\n      this.pollingManager.reset();\n      this.sockets = [];\n    }\n  }]);\n\n  return Penta;\n}();\n\nPenta.prototype.BigNumber = utils.BigNumber;\nPenta.prototype.toHex = utils.toHex;\nPenta.prototype.toAscii = utils.toAscii;\nPenta.prototype.fromAscii = utils.fromAscii;\nPenta.prototype.toDecimal = utils.toDecimal;\nPenta.prototype.fromDecimal = utils.fromDecimal;\nPenta.prototype.toBigNumber = utils.toBigNumber;\nPenta.prototype.isAddress = utils.isAddress;\nPenta.prototype.toU = utils.toU;\nPenta.prototype.fromU = utils.fromU;\n// Returns a normalized SHA3-256 hash of the given data.\nPenta.prototype.sha3 = function (params) {\n  return this._request.send(Jsonrpc.toPayload(\"util_sha3\", [params]));\n};\n\nmodule.exports = Penta;\n\n//# sourceURL=webpack://Penta/./lib/penta.js?");

/***/ }),

/***/ "./lib/penta/account.js":
/*!******************************!*\
  !*** ./lib/penta/account.js ***!
  \******************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * Create and manage penta account\n * @class Account\n * @author Buddywangw <buddywangw@gmail.com>\n */\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar Account = function () {\n  function Account(penta) {\n    _classCallCheck(this, Account);\n\n    this._request = penta._request;\n  }\n\n  /**\n   * Returns a list of addresses.\n   * @returns {array} - a list of addresses\n   */\n\n\n  _createClass(Account, [{\n    key: \"listAccounts\",\n    value: function listAccounts() {\n      return this._request.send(Jsonrpc.toPayload(\"account_listAccounts\"));\n    }\n\n    /**\n     * Creates a new account\n     * @param {string} passphrase - The account password that was set when the account was created\n     * @param {string} safecode  - The security password that was set when the account was created\n     * @returns {string} Create a new account with Base58 encoded address\n     */\n\n  }, {\n    key: \"newAccount\",\n    value: function newAccount(passphrase, safecode) {\n      return this._request.send(Jsonrpc.toPayload(\"account_newAccount\", [passphrase, safecode]));\n    }\n\n    /**\n     * Unlock an account.\n     * @param {string} address\n     * @param {string} passphrase - Account password that was set when creating an account\n     * @param {string} duration - Unlock time\n     * @returns {boolean} - Processing result, true is success, false is failure\n     */\n\n  }, {\n    key: \"unlockAccount\",\n    value: function unlockAccount(address, passphrase, duration) {\n      return this._request.send(Jsonrpc.toPayload(\"account_unlockAccount\", [address, passphrase, duration]));\n    }\n\n    /**\n     * Lock an account.\n     * @param {string} address\n     * @returns {boolean} - Processing result, true is success, false is failure\n     */\n\n  }, {\n    key: \"lockAccount\",\n    value: function lockAccount(address) {\n      return this._request.send(Jsonrpc.toPayload(\"account_lockAccount\", [address]));\n    }\n\n    /**\n     * Returns Account information\n     * @param {string} address\n     * @return {object} Returns Account object information  {\n     *  url: address inside keystone\n     * }\n     */\n\n  }, {\n    key: \"account\",\n    value: function account(address) {\n      return this._request.send(Jsonrpc.toPayload(\"account_account\", [address]));\n    }\n\n    /**\n     * Creates a new specified key\n     * @param {string} address\n     * @param {string} keyType tx、consensus、seed\n     * @param {string} passphrase - Account password\n     * @returns {string}  Returns the MAC of the newly generated key\n     */\n\n  }, {\n    key: \"newKey\",\n    value: function newKey(address, keyType, passphrase) {\n      return this._request.send(Jsonrpc.toPayload(\"account_newKey\", [address, keyType, passphrase]));\n    }\n\n    /**\n     * Creates a new OwnerKey.\n     * @param {string} address\n     * @param {string} passphrase - Account password\n     * @param {string} safecode - Security password\n     * @returns {string} Returns the MAC of the newly generated ownerkey\n     */\n\n  }, {\n    key: \"newOwnerKey\",\n    value: function newOwnerKey(address, passphrase, safecode) {\n      return this._request.send(Jsonrpc.toPayload(\"account_newOwnerKey\", [address, passphrase, safecode]));\n    }\n\n    /**\n     * Updates the key according to the specified type\n     * @param {object} tranObj - A transaction object\n     * @param {string} keyType - tx, consensus, seed\n     * @param {string} passphrase - Account password\n     * @param {string} safecode - Security password\n     * @returns {string} 32 Bytes - the transaction hash, or the zero hash if the transaction is not yet available.\n     */\n\n  }, {\n    key: \"updateKey\",\n    value: function updateKey(tranObj, keyType, passphrase, safecode) {\n      return this._request.send(Jsonrpc.toPayload(\"account_updateKey\", [tranObj, keyType, passphrase, safecode]));\n    }\n\n    /**\n     * Imports Account and input as json string.\n     * @param {string} account - JSON string\n     * @param {string} orgPassphrase - Account password\n     * @param {string} orgSafecode - Security password\n     * @param {string} passphrase \b- Import account password (corresponding to export account password)\n     * @param {string} safecode - Import security password (corresponding to export security password)\n     * @param {string} keyType - array of key types: \"seed,tx,consensus,owner,all\", empty or \"all\" means all\n     * @param {boolean} flag - Overwrite flag\n     * @return {string} Returns Account object information\n     */\n\n  }, {\n    key: \"importFromJson\",\n    value: function importFromJson(account, orgPassphrase, orgSafecode, passphrase, safecode, keyType, flag) {\n      return this._request.send(Jsonrpc.toPayload(\"account_importFromJson\", [account, orgPassphrase, orgSafecode, passphrase, safecode, keyType, flag]));\n    }\n\n    /**\n     * Imports Account and enter the json string. Only local file operations are supported\n     * @param {string} filename - filename\n     * @param {string} orgPassphrase - Original account password\n     * @param {string} orgSafecode - Original security password\n     * @param {string} passphrase \b- Import account password (corresponding to export account password)\n     * @param {string} safecode - Import security password (corresponding to export security password)\n     * @param {string} keyType \b- array of key types: \"seed, tx, consensus, owner, all\", empty or \"all\" means all\n     * @param {string} flag - Overwrite flag\n     * @returns {string} Returns Account object information\n     */\n\n  }, {\n    key: \"importFromFile\",\n    value: function importFromFile(filename, orgPassphrase, orgSafecode, passphrase, safecode, keyType, flag) {\n      return this._request.send(Jsonrpc.toPayload(\"account_importFromFile\", [filename, orgPassphrase, orgSafecode, passphrase, safecode, keyType, flag]));\n    }\n\n    /**\n     * Export the account key and output it as a json string.\n     * @param {string} address - address\n     * @param {string} passphrase - Account password\n     * @param {string} safecode 安全密码 - Security password\n     * @param {string} expassphrase - Export account password (which was used during the import function)\n     * @param {string} exsafecode - Export security password (which was used during the import function)\n     * @param {string} keyType  - array of key types: \"seed, tx, consensus, owner, all\", empty or \"all\" means all\n     * @param {boolean} flag -  Delete flag\n     * @returns {string} Returns account key information, json format\n     */\n\n  }, {\n    key: \"exportToJson\",\n    value: function exportToJson(address, passphrase, safecode, expassphrase, exsafecode, keyType, flag) {\n      return this._request.send(Jsonrpc.toPayload(\"account_exportToJson\", [address, passphrase, safecode, expassphrase, exsafecode, keyType, flag]));\n    }\n\n    /**\n     * Exports the account and output it as a file. Only local file operations are supported\n     * @param {string} address - address\n     * @param {string} filePath - FilePath\n     * @param {string} passphrase - Account password\n     * @param {string} safecode - Security password\n     * @param {string} expassphrase - Export account password (which was used during the import function)\n     * @param {string} exsafecode - Export security password (which was used during the import function)\n     * @param {string} keyType - array of key types: \"seed, tx, consensus, owner, all\", empty or \"all\" means all\n     * @param {boolean} flag - Delete flag, delete original key\n     * @return {file} \bFile name: FilePath+FileName(hex account address).json\n    If the file exists, it will be overwritten and the original content will be lost.\n     */\n\n  }, {\n    key: \"exportToFile\",\n    value: function exportToFile(address, filePath, passphrase, safecode, expassphrase, exsafecode, keyType, flag) {\n      return this._request.send(Jsonrpc.toPayload(\"account_exportToFile\", [address, filePath, passphrase, safecode, expassphrase, exsafecode, keyType, flag]));\n    }\n\n    /**\n     * Signed transaction data (only account password required).\n     * @param {string} data - Data to sign\n     * @param {string} accoutMac - Account signature key's MAC\n     * @param {string} keyType `- Type of account signature key: \"seed, tx, consensus\"\n     * @param {string} address - address\n     * @param {string} passphrase - Account password set when creating an account\n     * @returns {string} - Signed data\n     */\n\n  }, {\n    key: \"sign\",\n    value: function sign(data, accoutMac, keyType, address, passphrase) {\n      return this._request.send(Jsonrpc.toPayload(\"account_sign\", [data, accoutMac, address, passphrase]));\n    }\n\n    /**\n     * Signature data (requires account password and security password, i.e. owner key signature).\n     * @param {string} data  - Data to sign\n     * @param {string} accoutMac  `- Account signature key MAC, can be empty\n     * @param {string} address - address\n     * @param {string} passphrase - Account password set when creating an account\n     * @param {string} safeCode - Security password set when the account was created\n     */\n\n  }, {\n    key: \"signWithSecurity\",\n    value: function signWithSecurity(data, accoutMac, address, passphrase, safeCode) {\n      return this._request.send(Jsonrpc.toPayload(\"account_signWithSecurity\", [data, accoutMac, address, passphrase, safeCode]));\n    }\n\n    /**\n     * If the data field contains code, create new message call transaction or a contract creation.\n     * @param {object} tranObj - The transcation object\n     * @param {string} passwordAccount - The account password\n     * @param {string} keyMac  - MAC of the account key (need to be detected from the ledger)\n     * @param {string} keyType  - Type of account signature key：\"seed,tx,consensus\"\n     * @returns {string} 32 Bytes - the transaction hash, or the zero hash if the transaction is not yet available.\n     */\n\n  }, {\n    key: \"sendTransaction\",\n    value: function sendTransaction(tranObj, passwordAccount, keyMac, keyType) {\n      return this._request.send(Jsonrpc.toPayload(\"account_sendTransaction\", [tranObj, passwordAccount, keyMac, keyType]));\n    }\n\n    /**\n     * Creates a new message to invoke the transaction or create a contract, if the data field contains code.\n     * @param {object} tranObj - The transaction object\n     * @param {string} passwordAccount - account password\n     * @param {string} keyMac - The account key MAC (need a query from the chain)\n     * @param {string} keyType  - Type of account signature key：\b\"\bseed,tx,consensus\"\n     * @returns {object} 32 Byte - a transaction hash, or empty if the transaction is not available.\n     */\n\n  }, {\n    key: \"signTransaction\",\n    value: function signTransaction(tranObj, passwordAccount, keyMac, keyType) {\n      return this._request.send(Jsonrpc.toPayload(\"account_signTransaction\", [tranObj, passwordAccount, keyMac, keyType]));\n    }\n\n    /**\n     * If the data field contains code, create new message call transaction or a contract creation (using the owner key to sign the transaction).\n     * @param {string} tranObj - A transaction object\n     * @param {string} passwordAccount - The account password\n     * @param {string} passwordSecurity - The security password\n     * @param {string} keyMac  - The account key MAC (need a query from the chain), can be null\n     * @returns {object} 32 bytes -the transaction hash, or the zero hash if the transaction is not yet available.\n     */\n\n  }, {\n    key: \"signTransactionWithSecurity\",\n    value: function signTransactionWithSecurity(tranObj, passwordAccount, passwordSecurity, keyMac) {\n      return this._request.send(Jsonrpc.toPayload(\"account_signTransactionWithSecurity\", [tranObj, passwordAccount, passwordSecurity, keyMac]));\n    }\n\n    /**\n     * If the data field contains code, create new message call transaction or a contract creation (using the owner key to sign the transaction).\n     * @param {string} tranObj - A transaction object\n     * @param {string} passwordAccount - The account password\n     * @param {string} passwordSecurity - The security password\n     * @param {string} keyMac  - MAC of the account key, can be null (need to be detected from the ledger)\n     * @returns {string} 32 Bytes - the transaction hash, or the zero hash if the transaction is not yet available.\n     */\n\n  }, {\n    key: \"sendTransactionWithSecurity\",\n    value: function sendTransactionWithSecurity(tranObj, passwordAccount, passwordSecurity, keyMac) {\n      return this._request.send(Jsonrpc.toPayload(\"account_sendTransactionWithSecurity\", [tranObj, passwordAccount, passwordSecurity, keyMac]));\n    }\n\n    /**\n     * check whether account name exists\n     * @param {string} name - Account name\n     * @returns {boolean} - true:exist, false: not exist.\n     */\n\n  }, {\n    key: \"checkName\",\n    value: function checkName(name) {\n      return this._request.send(Jsonrpc.toPayload(\"account_checkName\", [name]));\n    }\n\n    /**\n     * Returns address depending on the account name.\n     * @param {string} name - Account name\n     * @returns {string}  address\n     */\n\n  }, {\n    key: \"getAddressByName\",\n    value: function getAddressByName(name) {\n      return this._request.send(Jsonrpc.toPayload(\"account_getAddressByName\", [name]));\n    }\n  }]);\n\n  return Account;\n}();\n\nmodule.exports = Account;\n\n//# sourceURL=webpack://Penta/./lib/penta/account.js?");

/***/ }),

/***/ "./lib/penta/admin.js":
/*!****************************!*\
  !*** ./lib/penta/admin.js ***!
  \****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * Admin Class\n * @class Admin\n * @author Buddywangw <buddywangw@gmail.com>\n */\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar Admin = function () {\n  function Admin(penta) {\n    _classCallCheck(this, Admin);\n\n    this._request = penta._request;\n  }\n\n  /**\n   * Returns a list of block producers\n   * @returns {array} - a list of block producers\n   */\n\n\n  _createClass(Admin, [{\n    key: \"peers\",\n    value: function peers() {\n      return this._request.send(Jsonrpc.toPayload(\"admin_peers\"));\n    }\n\n    //TODO:\n\n  }, {\n    key: \"netPeers\",\n    value: function netPeers(netId) {\n      return this._request.send(Jsonrpc.toPayload(\"admin_netPeers\", [netId]));\n    }\n\n    /**\n     * Add a new peer\n     * @param {string} url - enode url of a peer\n     * @returns {boolean} - true：added successfully  false: failed to add\n     */\n\n  }, {\n    key: \"addPeer\",\n    value: function addPeer(url) {\n      return this._request.send(Jsonrpc.toPayload(\"admin_addPeer\", [url]));\n    }\n\n    // TODO:\n\n  }, {\n    key: \"netAddPeer\",\n    value: function netAddPeer(netId, url) {\n      return this._request.send(Jsonrpc.toPayload(\"admin_netAddPeer\", [netId, url]));\n    }\n\n    /**\n     * Get absolute path to the storage database to the current node\n     * @returns {string} - Absolute path to the storage database\n     */\n\n  }, {\n    key: \"datadir\",\n    value: function datadir() {\n      return this._request.send(Jsonrpc.toPayload(\"admin_datadir\", null));\n    }\n\n    /**\n     * Get node information\n     * @returns {object} - node information\n     */\n\n  }, {\n    key: \"nodeInfo\",\n    value: function nodeInfo() {\n      return this._request.send(Jsonrpc.toPayload(\"admin_nodeInfo\"));\n    }\n\n    // TODO:\n\n  }, {\n    key: \"netNodeInfo\",\n    value: function netNodeInfo(netId) {\n      return this._request.send(Jsonrpc.toPayload(\"admin_netNodeInfo\", [netId]));\n    }\n\n    /**\n     * start HTTP RPC service\n     * @param {string} - Host\n     * @param {string} - Port\n     * @param {string} - Origin Resource Sharing\n     * @param {string} - apis\n     * @param {string} - Virtual Hosts\n     * @returns {boolean} - true: start successfully false: failed to start\n     */\n\n  }, {\n    key: \"startRPC\",\n    value: function startRPC(host, port, cors, apis, vhosts) {\n      return this._request.send(Jsonrpc.toPayload(\"admin_startRPC\", [host, port, cors, apis, vhosts]));\n    }\n\n    /**\n     * stop HTTP RPC service\n     * @returns {boolean} -  true:stop successfully false:failed to stop\n     */\n\n  }, {\n    key: \"stopRPC\",\n    value: function stopRPC() {\n      return this._request.send(Jsonrpc.toPayload(\"admin_stopRPC\"));\n    }\n\n    /**\n     * Start WebSocket RPC service\n     * @param {string} - Host\n     * @param {number} - Port\n     * @param {string} - Origin Resource Sharing\n     * @param {string} - apis\n     * @return {boolean} - true: start successfully false: failed to start\n     */\n\n  }, {\n    key: \"startWS\",\n    value: function startWS(host, port, cors, apis) {\n      return this._request.send(Jsonrpc.toPayload(\"admin_startWS\", [host, port, cors, apis]));\n    }\n\n    /**\n     * Stop WebSocket RPC service\n     * @returns {boolean} - true:stop successfully false:failed to stop\n     */\n\n  }, {\n    key: \"stopWS\",\n    value: function stopWS() {\n      return this._request.send(Jsonrpc.toPayload(\"admin_stopWS\"));\n    }\n  }]);\n\n  return Admin;\n}();\n\nmodule.exports = Admin;\n\n//# sourceURL=webpack://Penta/./lib/penta/admin.js?");

/***/ }),

/***/ "./lib/penta/asyncing.js":
/*!*******************************!*\
  !*** ./lib/penta/asyncing.js ***!
  \*******************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * Asyncing Class\n * @description asyncing penta node info\n * @class Asyncing\n * @author Buddywangw <buddywangw@gmail.com>\n */\n\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar Asyncing = function () {\n  function Asyncing(pollingManager, callback) {\n    _classCallCheck(this, Asyncing);\n\n    this.pollingManager = pollingManager;\n    this.lastSyncState = false;\n    this.callback = callback;\n    return this.start();\n  }\n\n  _createClass(Asyncing, [{\n    key: \"start\",\n    value: function start() {\n      var payload = Jsonrpc.toPayload(\"penta_syncing\", null);\n      this.pollId = payload.id;\n      return this.pollingManager.startSync(payload, this.callback, this.stop);\n    }\n  }, {\n    key: \"stop\",\n    value: function stop() {\n      if (this.pollId) {\n        this.pollingManager.stop(this.pollId);\n      }\n    }\n  }]);\n\n  return Asyncing;\n}();\n\nmodule.exports = Asyncing;\n\n//# sourceURL=webpack://Penta/./lib/penta/asyncing.js?");

/***/ }),

/***/ "./lib/penta/blockchain.js":
/*!*********************************!*\
  !*** ./lib/penta/blockchain.js ***!
  \*********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * Blockchain Class\n * @class Blockchain\n * @author Buddywangw <buddywangw@gmail.com>\n */\nvar utils = __webpack_require__(/*! ../utils */ \"./lib/utils/index.js\");\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar Blockchain = function () {\n  function Blockchain(penta) {\n    _classCallCheck(this, Blockchain);\n\n    this._request = penta._request;\n  }\n\n  /**\n   * Returns the balance of the account of given address.\n   * @param {string} account - address\n   * @param {number|string} blockNumber - integer block number, or the string \"latest\", \"earliest\" or \"pending\"\n   * @returns {string} - Integer of the current balance of u.\n   */\n\n\n  _createClass(Blockchain, [{\n    key: \"getBalance\",\n    value: function getBalance(account, blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getBalance\", [account, blockNumber]));\n    }\n\n    /**\n     * Returns the account status of given address.\n     * @param {string} address - address\n     * @param {string} blockNumber - integer block number, or the string \"latest\", \"earliest\" or \"pending\"\n     * @return {boolean} - true: normal，false: frozen.\n     */\n\n  }, {\n    key: \"isAccountNormal\",\n    value: function isAccountNormal(address, blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_isAccountNormal\", [address, blockNumber]));\n    }\n\n    /**\n     * Returns the MAC information of account keys in current statedb.\n     * @param {string} address - address\n     * @returns {object} - the MAC information\n     * {\n     *  consensusKey\n     *  ownerKey\n     *  txKey\n     *  seedKey1\n     *  seedKey1Number - the block number when seed key 1 works\n     *  seedKey2\n     *  seedKey2Number - the block number when seed key 2 works\n     * }\n     */\n\n  }, {\n    key: \"getStateKeys\",\n    value: function getStateKeys(address) {\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getStateKeys\", [address]));\n    }\n\n    /**\n     * Returns the account information in current statedb.\n     * @param {string} address - address\n     * @returns {object} - account information\n     * {\n     *   consensusKey\n     *   name - Account name\n     *   ownerKey\n     *   seedKey\n     *   txKey\n     *   isNormal - The account is nomarl or not：true - normal，false - frozen\n     *   balance\n     * }\n     */\n\n  }, {\n    key: \"getAccountInfo\",\n    value: function getAccountInfo(address) {\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getAccountInfo\", [address]));\n    }\n\n    /**\n     * Returns contract code at a given address.\n     * @param {string} account - address\n     * @param {string} blockNumber - integer block number, or the string \"latest\", \"earliest\" or \"pending\"\n     * @returns {string} - the code from the given address.\n     */\n\n  }, {\n    key: \"getCode\",\n    value: function getCode(account, blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getCode\", [account, blockNumber]));\n    }\n\n    /**\n     * Returns the value from a storage position at a given address.\n     * @param {string} address - 20 Bytes address.\n     * @param {string} index - the integer at this storage position.\n     * @param {string} blockNumber - integer block number, or the string \"latest\", \"earliest\" or \"pending\"\n     * @returns {string} - the integer at this storage position.\n     */\n\n  }, {\n    key: \"getStorageAt\",\n    value: function getStorageAt(address, index, blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getStorageAt\", [address, index, blockNumber]));\n    }\n\n    /**\n     * Returns the number of most recent block.\n     * @returns {string} - integer of the current block number the client is on.\n     */\n\n  }, {\n    key: \"blockNumber\",\n    value: function blockNumber() {\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_blockNumber\", null));\n    }\n\n    /**\n     * Returns information about a block by block number.\n     * @param {string} blockNumber - integer of a block number, or the string \"earliest\", \"latest\" or \"pending\", as in the default block parameter.\n     * @param {boolean} flag - If true it returns the full transaction objects, if false only the hashes of the transactions.\n     * @returns {object}  - A block object, or null when no block was found\n     */\n\n  }, {\n    key: \"getBlockByNumber\",\n    value: function getBlockByNumber(blockNumber, flag) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getBlockByNumber\", [blockNumber, flag]));\n    }\n\n    /**\n     * Returns the information about a transaction requested by transaction hash.\n     * @param {string} hash - 32 Bytes（hash of a transaction）\n     * @param {boolean} flag - If true it returns the full transaction objects, if false only the hashes of the transactions.\n     * @returns {object} - A block object, or null when no block was found\n     */\n\n  }, {\n    key: \"getBlockByHash\",\n    value: function getBlockByHash(hash, flag) {\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getBlockByHash\", [hash, flag]));\n    }\n\n    /**\n     * Returns information about an uncle of a block by number and uncle index position.\n     * @param {string} blockNumber - a block number, or the string \"earliest\", \"latest\" or \"pending\"\n     * @param {string} uncleIndex - the uncle's index position.\n     * @returns {object} A block object, or null when no block was found\n     */\n\n  }, {\n    key: \"getUncleByBlockNumberAndIndex\",\n    value: function getUncleByBlockNumberAndIndex(blockNumber, uncleIndex) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getUncleByBlockNumberAndIndex\", [blockNumber, uncleIndex]));\n    }\n\n    /**\n     * Returns information about a uncle of a block by hash and uncle index position.\n     * @param {string} blockHash - 32 Bytes(block hash).\n     * @param {string} index - the uncle's index position.\n     * @returns {object} - A block object, or null when no block was found\n     */\n\n  }, {\n    key: \"getUncleByBlockHashAndIndex\",\n    value: function getUncleByBlockHashAndIndex(blockHash, index) {\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getUncleByBlockHashAndIndex\", [blockHash, index]));\n    }\n\n    /**\n     * Returns the number of uncles in a block from a block matching the given block number.\n     * @param {string} blockNumber - integer block number, or the string \"latest\", \"earliest\" or \"pending\"\n     * @returns {string} - integer of the number of uncles in this block.\n     */\n\n  }, {\n    key: \"getUncleCountByBlockNumber\",\n    value: function getUncleCountByBlockNumber(blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getUncleCountByBlockNumber\", [blockNumber]));\n    }\n\n    /**\n     * Returns the number of uncles in a block from a block matching the given block hash.\n     * @param {string} blockHash - 32 Bytes(block hash)\n     * @returns {string} - integer of the number of uncles in this block.\n     */\n\n  }, {\n    key: \"getUncleCountByBlockHash\",\n    value: function getUncleCountByBlockHash(blockHash) {\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_getUncleCountByBlockHash\", [blockHash]));\n    }\n\n    /**\n     * Executes a new call message immediately without creating a transaction on the block chain.\n     * @param {object} tranObject - The transaction call object\n     * @param {string} blockNumber - integer block number, or the string \"latest\", \"earliest\" or \"pending\"\n     * @returns {string} - The return value of executed contract.\n     */\n\n  }, {\n    key: \"call\",\n    value: function call(tranObject, blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_call\", [tranObject, blockNumber]));\n    }\n\n    /**\n     * Generates and returns an estimate of how much gas is necessary to allow the transaction to complete.\n     * @param {object} tranObject - The transaction call object\n     * @param {string} blockNumber - integer block number, or the string \"latest\", \"earliest\" or \"pending\"\n     * @returns {string} - the amount of gas used.\n     */\n\n  }, {\n    key: \"estimateGas\",\n    value: function estimateGas(tranObject, blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"blockchain_estimateGas\", [tranObject, blockNumber]));\n    }\n  }]);\n\n  return Blockchain;\n}();\n\nmodule.exports = Blockchain;\n\n//# sourceURL=webpack://Penta/./lib/penta/blockchain.js?");

/***/ }),

/***/ "./lib/penta/filter.js":
/*!*****************************!*\
  !*** ./lib/penta/filter.js ***!
  \*****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nvar utils = __webpack_require__(/*! ../utils */ \"./lib/utils/index.js\");\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar toTopic = function toTopic(value) {\n  if (value === null || typeof value === \"undefined\") return null;\n\n  value = String(value);\n\n  if (value.indexOf(\"0x\") === 0) return value;else return utils.fromUtf8(value);\n};\n\nvar getOptions = function getOptions(options) {\n  if (utils.isString(options)) {\n    return options;\n  }\n  options = options || {};\n  options.topics = options.topics || [];\n  options.topics = options.topics.map(function (topic) {\n    return utils.isArray(topic) ? topic.map(toTopic) : toTopic(topic);\n  });\n\n  return {\n    topics: options.topics,\n    from: options.from,\n    to: options.to,\n    address: options.address,\n    fromBlock: utils.inputBlockNumberFormatter(options.fromBlock),\n    toBlock: utils.inputBlockNumberFormatter(options.toBlock)\n  };\n};\n\n/**\n * Filter Class.\n * @description newPendingTransactions|newHeads|logs Please use websoket\n * @class Filter\n * @author Buddywangw <buddywangw@gmail.com>\n */\n\nvar Filter = function () {\n  function Filter(penta, options, callback) {\n    var _this = this;\n\n    _classCallCheck(this, Filter);\n\n    this.options = getOptions(options);\n    this._request = penta._request;\n    this.pollingManager = penta.pollingManager;\n    this.filterId = \"\";\n    this.newFilter(this.options).then(function (result) {\n      _this.filterId = result;\n      callback();\n    });\n    return this;\n  }\n\n  _createClass(Filter, [{\n    key: \"newFilter\",\n    value: function newFilter(options) {\n      var filterType = \"\";\n      var params = options;\n      switch (options) {\n        case \"latest\":\n          filterType = \"filter_newBlockFilter\";\n          params = null;\n          break;\n        case \"pending\":\n          filterType = \"filter_newPendingTransactionFilter\";\n          params = null;\n          break;\n        default:\n          filterType = \"filter_newFilter\";\n      }\n      return this._request.send(Jsonrpc.toPayload(filterType, params));\n    }\n  }, {\n    key: \"getLogs\",\n    value: function getLogs(filterId) {\n      return this._request.send(Jsonrpc.toPayload(\"filter_getFilterLogs\", [filterId]));\n    }\n  }, {\n    key: \"watch\",\n    value: function watch(callback) {\n      this.callback = callback;\n\n      if (this.filterId) {\n        this.getLogsAtStart(callback);\n        this.pollFilter();\n      }\n\n      return this;\n    }\n  }, {\n    key: \"pollFilter\",\n    value: function pollFilter() {\n      var payload = Jsonrpc.toPayload(\"filter_getFilterChanges\", [this.filterId]);\n      this.pollId = payload.id;\n      return this.pollingManager.startSync(payload, this.callback, this.stop);\n    }\n  }, {\n    key: \"stop\",\n    value: function stop() {\n      if (this.pollingManager) {\n        this.pollingManager.stop(this.pollId);\n        return this._request.send(Jsonrpc.toPayload(\"filter_uninstallFilter\", [this.filterId]));\n      }\n    }\n  }, {\n    key: \"getLogsAtStart\",\n    value: function getLogsAtStart(callback) {\n      if (!utils.isString(this.options)) {\n        this.get(function (messages) {\n          if (utils.isArray(messages)) {\n            messages.forEach(function (message) {\n              callback(message);\n            });\n          }\n        });\n      }\n    }\n  }, {\n    key: \"get\",\n    value: function get(callback) {\n      if (this.filterId === null) {\n        this.getLogsCallbacks.push(callback);\n      } else {\n        this.getLogs(this.filterId).then(function (res) {\n          callback(res);\n        });\n      }\n    }\n  }]);\n\n  return Filter;\n}();\n\nmodule.exports = Filter;\n\n//# sourceURL=webpack://Penta/./lib/penta/filter.js?");

/***/ }),

/***/ "./lib/penta/miner.js":
/*!****************************!*\
  !*** ./lib/penta/miner.js ***!
  \****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * Miner Class.\n * @class Miner\n * @author Buddywangw <buddywangw@gmail.com>\n */\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar Miner = function () {\n  function Miner(penta) {\n    _classCallCheck(this, Miner);\n\n    this._request = penta._request;\n  }\n  /**\n   * Returns the client coinbase address (reward address).\n   * @returns {string}  the current coinbase address.\n   */\n\n\n  _createClass(Miner, [{\n    key: \"getCoinbase\",\n    value: function getCoinbase() {\n      return this._request.send(Jsonrpc.toPayload(\"miner_getCoinbase\", null));\n    }\n\n    /**\n     * \bSet the minimum acceptable gas price, any transaction fee that is less than this price is excluded by miners.\n     * @param {string} price - gas price.\n     * @returns {boolean} - true if submitting went through successfully and false otherwise.\n     */\n\n  }, {\n    key: \"setGasPrice\",\n    value: function setGasPrice(price) {\n      return this._request.send(Jsonrpc.toPayload(\"miner_setGasPrice\", [price]));\n    }\n\n    /**\n     * Sets the amount of additional data that a miner can add in a block, capped at 32 bytes.\n     * @param {string} extraData  - additional data\n     * @returns {boolean} - true if submitting went through successfully and false otherwise.\n     */\n\n  }, {\n    key: \"setExtra\",\n    value: function setExtra(extraData) {\n      return this._request.send(Jsonrpc.toPayload(\"miner_setExtra\", [extraData]));\n    }\n\n    /**\n     * get the number of hash values calculated every second in the current mining node\n     * @returns {number} uint64\n     */\n\n  }, {\n    key: \"getHashrate\",\n    value: function getHashrate() {\n      return this._request.send(Jsonrpc.toPayload(\"miner_getHashrate\"));\n    }\n\n    /**\n     * Initiate mining\n     * @param {number} threads -  int thread count\n     * @returns - if no information returns after normal start\n     */\n\n  }, {\n    key: \"start\",\n    value: function start(threads) {\n      return this._request.send(Jsonrpc.toPayload(\"miner_start\", [threads]));\n    }\n\n    /**\n     * cease mining\n     * @returns {boolean} - true:  cease mining successfully false: failed to cease mining\n     */\n\n  }, {\n    key: \"stop\",\n    value: function stop() {\n      return this._request.send(Jsonrpc.toPayload(\"miner_stop\"));\n    }\n\n    /**\n     * \bSet the address as Coinbase(reward address)\n     * @param {string} address - address\n     * @returns {boolean} - true if submitting went through successfully and false otherwise.\n     */\n\n  }, {\n    key: \"setCoinbase\",\n    value: function setCoinbase(address) {\n      return this._request.send(Jsonrpc.toPayload(\"miner_setCoinbase\", [address]));\n    }\n  }]);\n\n  return Miner;\n}();\n\nmodule.exports = Miner;\n\n//# sourceURL=webpack://Penta/./lib/penta/miner.js?");

/***/ }),

/***/ "./lib/penta/net.js":
/*!**************************!*\
  !*** ./lib/penta/net.js ***!
  \**************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * PentaNet Class.\n * @class PentaNet\n * @author Buddywangw <buddywangw@gmail.com>\n */\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar PentaNet = function () {\n  function PentaNet(penta) {\n    _classCallCheck(this, PentaNet);\n\n    this._request = penta._request;\n  }\n  /**\n   * Determining whether to connect to nodes\n   * @returns {boolean} true:connected，false：disconnected\n   */\n\n\n  _createClass(PentaNet, [{\n    key: \"isConnected\",\n    value: function isConnected() {\n      return this._request.isConnected();\n    }\n    /**\n     * Returns number of peers currently connected to the client.\n     * @returns {string}  - integer of the number of connected peers.\n     */\n\n  }, {\n    key: \"peerCount\",\n    value: function peerCount() {\n      return this._request.send(Jsonrpc.toPayload(\"net_peerCount\", null));\n    }\n\n    /**\n     * true if client is actively listening for network connections.\n     * @returns {boolean} - true when listening, otherwise false.\n     */\n\n  }, {\n    key: \"listening\",\n    value: function listening() {\n      return this._request.send(Jsonrpc.toPayload(\"net_listening\", null));\n    }\n  }]);\n\n  return PentaNet;\n}();\n\nmodule.exports = PentaNet;\n\n//# sourceURL=webpack://Penta/./lib/penta/net.js?");

/***/ }),

/***/ "./lib/penta/protocol.js":
/*!*******************************!*\
  !*** ./lib/penta/protocol.js ***!
  \*******************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nvar Asyncing = __webpack_require__(/*! ./asyncing */ \"./lib/penta/asyncing.js\");\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\n/**\n * Protocol Class.\n * @class Protocol\n */\n\nvar Protocol = function () {\n  function Protocol(penta) {\n    _classCallCheck(this, Protocol);\n\n    this._pollingManager = penta.pollingManager;\n    this._request = penta._request;\n  }\n\n  /**\n   * Get current gasPrice\n   * @returns {string} - current gasPrice\n   * @author Buddywangw <buddywangw@gmail.com>\n   */\n\n\n  _createClass(Protocol, [{\n    key: \"gasPrice\",\n    value: function gasPrice() {\n      return this._request.send(Jsonrpc.toPayload(\"penta_gasPrice\", null));\n    }\n\n    /**\n     * block sync\n     * @param {function} callback\n     * @returns {object|boolean} - Returns an object with data about the sync status or false.\n     */\n\n  }, {\n    key: \"syncing\",\n    value: function syncing(callback) {\n      return new Asyncing(this._pollingManager, callback);\n    }\n\n    /**\n     * Returns the current penta protocol version\n     * @returns {string} - current penta protocol version\n     */\n\n  }, {\n    key: \"protocolVersion\",\n    value: function protocolVersion() {\n      return this._request.send(Jsonrpc.toPayload(\"penta_protocolVersion\", null));\n    }\n\n    /**\n     * Returns the current node version\n     * @returns {string} - current node version\n     */\n\n  }, {\n    key: \"nodeVersion\",\n    value: function nodeVersion() {\n      return this._request.send(Jsonrpc.toPayload(\"penta_nodeVersion\", null));\n    }\n\n    /**\n     * Returns the current chain id\n     * @returns {string} - current chain id\n     */\n\n  }, {\n    key: \"chainId\",\n    value: function chainId() {\n      return this._request.send(Jsonrpc.toPayload(\"penta_chainId\", null));\n    }\n\n    /**\n     * Participant (delegate or witness) application\n     * @param {object} tranObj  -  The transcation object\n     * @param {string} passphrase - the account password\n     * @param {string} safeCode - the security password\n     * @returns {string} - 32 Bytes (the transaction hash, or the zero hash if the transaction is not yet available).\n     */\n\n  }, {\n    key: \"participantApply\",\n    value: function participantApply(tranObj, passphrase, safeCode) {\n      return this._request.send(Jsonrpc.toPayload(\"penta_participantApply\", [tranObj, passphrase, safeCode]));\n    }\n\n    /**\n     * Returns a list of proposal parameters\n     * @returns {array} -  a list of Proposal object\n     */\n\n  }, {\n    key: \"getProposalArgsList\",\n    value: function getProposalArgsList() {\n      return this._request.send(Jsonrpc.toPayload(\"penta_getProposalArgsList\", null));\n    }\n\n    /**\n     * Returns a list of Proposal information\n     * @param {string} pipStatus - Status of Proposal (Optional). 0-Voting; 1-Pending; 2-Effective; 3-Expired\n     * @param {sting} pipType - Proposal Type (optional). param- Proposals of configuration arguments modification; pip - Proposals of processing logic modification\n     * @param {sting} pageType - Page type. 01- Current proposal；02- Historical proposal；03- My proposal\n     * @param {sting} from - Current address\n     * @returns {object} - a list of Proposal properties\n     */\n\n  }, {\n    key: \"listProposals\",\n    value: function listProposals(_ref) {\n      var pipStatus = _ref.pipStatus,\n          pipType = _ref.pipType,\n          pageType = _ref.pageType,\n          from = _ref.from;\n\n      return this._request.send(Jsonrpc.toPayload(\"penta_listProposals\", [{\n        pipStatus: pipStatus,\n        pipType: pipType,\n        pageType: pageType,\n        from: from\n      }]));\n    }\n\n    /**\n     * Vote for a delegate\n     * @param {object} tranObj - The transcation object\n     * @param {string} passphrase - The account password\n     * @param {string} safeCode - The security password\n     * @returns {string} -32 Bytes(the transaction hash, or the zero hash if the transaction is not yet available).\n     */\n\n  }, {\n    key: \"delegateVote\",\n    value: function delegateVote(tranObj, passphrase, safeCode) {\n      return this._request.send(Jsonrpc.toPayload(\"penta_delegateVote\", [tranObj, passphrase, safeCode]));\n    }\n\n    /**\n     * Returns a list of delegates\n     * @param {string} address - current account address\n     * @param {types} types - empty: all delegates / 0: members / 1: observers\n     * @returns {array} a list of delegate properties\n     */\n\n  }, {\n    key: \"listParticipants\",\n    value: function listParticipants(address, types) {\n      return this._request.send(Jsonrpc.toPayload(\"penta_listParticipants\", [address, types]));\n    }\n\n    /**\n     * a list of delegate objects\n     * @param {string} address - current account address\n     * @returns {array} - a list of delegate objects\n     */\n\n  }, {\n    key: \"participant\",\n    value: function participant(address) {\n      return this._request.send(Jsonrpc.toPayload(\"penta_participant\", [address]));\n    }\n\n    /**\n     * Returns a list of historical voting records\n     * @param {number} voteType - 0:Delegate/1:Witness\n     * @param {array} addresses - account address list\n     * @returns {array} - List of delegate properites\n     */\n\n  }, {\n    key: \"listHistoryVotes\",\n    value: function listHistoryVotes(voteType, addresses) {\n      return this._request.send(Jsonrpc.toPayload(\"penta_listHistoryVotes\", [voteType, addresses]));\n    }\n\n    /**\n     * Apply for a proposal\n     * @param {string} proposal - proposal A proposal object\n     * @param {string} passphrase - The account password\n     * @param {string} safeCode - The security password\n     * @returns {string} 32 Bytes - the transaction hash, or the zero hash if the transaction is not yet available.\n     */\n\n  }, {\n    key: \"applyProposal\",\n    value: function applyProposal(proposal, passphrase, safeCode) {\n      return this._request.send(Jsonrpc.toPayload(\"penta_applyProposal\", [proposal, passphrase, safeCode]));\n    }\n\n    /**\n     * Vote for a Proposal\n     * @param {string} tranObj - A transaction object\n     * @param {string} passphrase - The account password\n     * @param {string} safeCode - The security password\n     * @returns {string} 32 Bytes - the transaction hash, or the zero hash if the transaction is not yet available.\n     */\n\n  }, {\n    key: \"proposalVote\",\n    value: function proposalVote(tranObj, passphrase, safeCode) {\n      return this._request.send(Jsonrpc.toPayload(\"penta_proposalVote\", [tranObj, passphrase, safeCode]));\n    }\n  }]);\n\n  return Protocol;\n}();\n\nmodule.exports = Protocol;\n\n//# sourceURL=webpack://Penta/./lib/penta/protocol.js?");

/***/ }),

/***/ "./lib/penta/tx.js":
/*!*************************!*\
  !*** ./lib/penta/tx.js ***!
  \*************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * Tx Class.\n * @class Tx\n * @author Buddywangw <buddywangw@gmail.com>\n */\nvar utils = __webpack_require__(/*! ../utils */ \"./lib/utils/index.js\");\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar Tx = function () {\n  function Tx(penta) {\n    _classCallCheck(this, Tx);\n\n    this._request = penta._request;\n  }\n  /**\n   * Returns the number of transactions in a block matching the given block number.\n   * @param {number|string} blockNumber - integer of a block number, or the string \"earliest\", \"latest\" or \"pending\", as in the default block parameter.\n   * @returns {number} - integer of the number of transactions in this block.\n   */\n\n\n  _createClass(Tx, [{\n    key: \"getBlockTransactionCountByNumber\",\n    value: function getBlockTransactionCountByNumber(blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"tx_getBlockTransactionCountByNumber\", [blockNumber]));\n    }\n\n    /**\n     * Returns the number of transactions in a block from a block matching the given block hash.\n     * @param {string} hash - 32 Bytes(block hash)\n     * @returns {number} - integer of the number of transactions in this block.\n     */\n\n  }, {\n    key: \"getBlockTransactionCountByHash\",\n    value: function getBlockTransactionCountByHash(hash) {\n      return this._request.send(Jsonrpc.toPayload(\"tx_getBlockTransactionCountByHash\", [hash]));\n    }\n\n    /**\n     * Returns information about a transaction by block number and transaction index position.\n     * @param {string} blockNumber - a block number, or the string \"earliest\", \"latest\" or \"pending\", as in the default block parameter.\n     * @param {string} tranIndex - the transaction index position.\n     * @returns {object} A transaction object, or null when no transaction was found\n     */\n\n  }, {\n    key: \"getTransactionByBlockNumberAndIndex\",\n    value: function getTransactionByBlockNumberAndIndex(blockNumber, tranIndex) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"tx_getTransactionByBlockNumberAndIndex\", [blockNumber, tranIndex]));\n    }\n\n    /**\n     * Returns information about a transaction by block hash and transaction index position.\n     * @param {string} blockHash - 32 Bytes(block hash).\n     * @param {string} tranIndex - integer of the transaction index position.\n     * @returns {object} - A transaction object, or null when no transaction was found\n     */\n\n  }, {\n    key: \"getTransactionByBlockHashAndIndex\",\n    value: function getTransactionByBlockHashAndIndex(blockHash, tranIndex) {\n      return this._request.send(Jsonrpc.toPayload(\"tx_getTransactionByBlockNumberAndIndex\", [blockHash, tranIndex]));\n    }\n\n    /**\n     * Returns information about a transaction by block number and transaction index position.\n     * @param {string} blockNumber - a block number, or the string \"earliest\", \"latest\" or \"pending\", as in the default block parameter.\n     * @returns {array} - informations of all transaction objects in the block\n     */\n\n  }, {\n    key: \"getTransactionsByBlockNumber\",\n    value: function getTransactionsByBlockNumber(blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"tx_getTransactionsByBlockNumber\", [blockNumber]));\n    }\n\n    /**\n     * Returns information about a transaction by block hash.\n     * @param {string} blockHash - 32 bytes(hash of the block).\n     * @returns {array} - informations of all transaction objects in the block\n     */\n\n  }, {\n    key: \"getTransactionsByBlockHash\",\n    value: function getTransactionsByBlockHash(blockHash) {\n      return this._request.send(Jsonrpc.toPayload(\"tx_getTransactionsByBlockHash\", [blockHash]));\n    }\n\n    /**\n     * Returns a specific transaction with a given block number and index\n     * @param {string} blockNumber - Block number\n     * @param {string} index - index，uint type\n     * @returns {object} - Transaction information, RLP encoding\n     */\n\n  }, {\n    key: \"getRawTransactionByBlockNumberAndIndex\",\n    value: function getRawTransactionByBlockNumberAndIndex(blockNumber, index) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"tx_getRawTransactionByBlockNumberAndIndex\", [blockNumber, index]));\n    }\n\n    /**\n     * Returns a specific transaction with a given block hash and index\n     * @param {string} blockHash - 32 Bytes(block hash).\n     * @param {string} index - index，uint type\n     * @returns {object} - Transaction information, RLP encoding\n     */\n\n  }, {\n    key: \"getRawTransactionByBlockHashAndIndex\",\n    value: function getRawTransactionByBlockHashAndIndex(blockHash, index) {\n      return this._request.send(Jsonrpc.toPayload(\"tx_getRawTransactionByBlockHashAndIndex\", [blockHash, index]));\n    }\n\n    /**\n     * Returns the number of transactions sent from an address.\n     * @param {string} address - 20 Bytes(address).\n     * @param {string} blockNumber - integer block number, or the string \"latest\", \"earliest\" or \"pending\", see the default block parameter.\n     * @returns {string} - integer of the number of transactions send from this address.\n     */\n\n  }, {\n    key: \"getTransactionCount\",\n    value: function getTransactionCount(address, blockNumber) {\n      blockNumber = utils.inputBlockNumberFormatter(blockNumber);\n      return this._request.send(Jsonrpc.toPayload(\"tx_getTransactionCount\", [address, blockNumber]));\n    }\n\n    /**\n     * Returns the information about a transactions with the specific transaction hash.\n     * @param {string} hash - 32 Bytes(transaction hash)\n     * @returns {object} - A transaction object, or null when no transaction was found\n     */\n\n  }, {\n    key: \"getTransactionByHash\",\n    value: function getTransactionByHash(hash) {\n      return this._request.send(Jsonrpc.toPayload(\"tx_getTransactionByHash\", [hash]));\n    }\n\n    /**\n     * Return the specified transaction with the given hash\n     * @param {string} rawHash - 32 Bytes(block hash).\n     * @returns {object} - Transaction information, RLP encoding\n     */\n\n  }, {\n    key: \"getRawTransactionByHash\",\n    value: function getRawTransactionByHash(rawHash) {\n      return this._request.send(Jsonrpc.toPayload(\"tx_getRawTransactionByHash\", [rawHash]));\n    }\n\n    /**\n     * Returns the receipt of a transaction by transaction hash.\n     * @param {string} txHash - 32 Bytes(hash of a transaction)\n     * @returns {object}  A transaction receipt object, or null when no receipt was found\n     */\n\n  }, {\n    key: \"getTransactionReceipt\",\n    value: function getTransactionReceipt(txHash) {\n      return this._request.send(Jsonrpc.toPayload(\"tx_getTransactionReceipt\", [txHash]));\n    }\n\n    /**\n     * Creates new message call transaction or a contract creation for signed transactions.\n     * @param {object} rlpCode - RLP encoding of the Transaction object\n     * @returns {string} 32 Bytes - the transaction hash, or the zero hash if the transaction is not yet available.\n     */\n\n  }, {\n    key: \"sendRawTransaction\",\n    value: function sendRawTransaction(rlpCode) {\n      return this._request.send(Jsonrpc.toPayload(\"tx_sendRawTransaction\", [rlpCode]));\n    }\n  }]);\n\n  return Tx;\n}();\n\nmodule.exports = Tx;\n\n//# sourceURL=webpack://Penta/./lib/penta/tx.js?");

/***/ }),

/***/ "./lib/penta/txpool.js":
/*!*****************************!*\
  !*** ./lib/penta/txpool.js ***!
  \*****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * Txpool Class.\n * @class Txpool\n * @author Buddywangw <buddywangw@gmail.com>\n */\nvar Jsonrpc = __webpack_require__(/*! ../jsonrpc */ \"./lib/jsonrpc.js\");\n\nvar Txpool = function () {\n  function Txpool(penta) {\n    _classCallCheck(this, Txpool);\n\n    this._request = penta._request;\n  }\n  /**\n   * List the exact details of all transactions currently waiting for the next block, as well as the transactions scheduled to be executed in the future.\n   * @returns {array}  - Transaction details\n   */\n\n\n  _createClass(Txpool, [{\n    key: \"content\",\n    value: function content() {\n      return this._request.send(Jsonrpc.toPayload(\"txpool_content\", null));\n    }\n\n    /**\n     * Return the number of transactions contained in the next block, as well as the number of transactions scheduled to be executed in the future.\n     * @returns {number} - Number of transactions.\n     */\n\n  }, {\n    key: \"status\",\n    value: function status() {\n      return this._request.send(Jsonrpc.toPayload(\"txpool_status\", null));\n    }\n\n    /**\n     * Return a text summary list of all transactions currently waiting for the next block, as well as a text summary of the transactions scheduled to be executed in the future.\n     * @returns {object} -  a text summary list\n     */\n\n  }, {\n    key: \"inspect\",\n    value: function inspect() {\n      return this._request.send(Jsonrpc.toPayload(\"txpool_inspect\", null));\n    }\n  }]);\n\n  return Txpool;\n}();\n\nmodule.exports = Txpool;\n\n//# sourceURL=webpack://Penta/./lib/penta/txpool.js?");

/***/ }),

/***/ "./lib/plugins/bignumber.js":
/*!**********************************!*\
  !*** ./lib/plugins/bignumber.js ***!
  \**********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("var __WEBPACK_AMD_DEFINE_RESULT__;\n\nvar _typeof = typeof Symbol === \"function\" && typeof Symbol.iterator === \"symbol\" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === \"function\" && obj.constructor === Symbol && obj !== Symbol.prototype ? \"symbol\" : typeof obj; };\n\n/* bignumber.js v2.0.3 https://github.com/MikeMcl/bignumber.js/LICENCE */\n/* modified by zelig to fix https://github.com/robertkrimen/otto#regular-expression-incompatibility */\n!function (e) {\n  \"use strict\";\n  function n(e) {\n    function a(e, n) {\n      var t,\n          r,\n          i,\n          o,\n          u,\n          s,\n          f = this;if (!(f instanceof a)) return j && L(26, \"constructor call without new\", e), new a(e, n);if (null != n && H(n, 2, 64, M, \"base\")) {\n        if (n = 0 | n, s = e + \"\", 10 == n) return f = new a(e instanceof a ? e : s), U(f, P + f.e + 1, k);if ((o = \"number\" == typeof e) && 0 * e != 0 || !new RegExp(\"^-?\" + (t = \"[\" + O.slice(0, n) + \"]+\") + \"(?:\\\\.\" + t + \")?$\", 37 > n ? \"i\" : \"\").test(s)) return g(f, s, o, n);o ? (f.s = 0 > 1 / e ? (s = s.slice(1), -1) : 1, j && s.replace(/^0\\.0*|\\./, \"\").length > 15 && L(M, b, e), o = !1) : f.s = 45 === s.charCodeAt(0) ? (s = s.slice(1), -1) : 1, s = D(s, 10, n, f.s);\n      } else {\n        if (e instanceof a) return f.s = e.s, f.e = e.e, f.c = (e = e.c) ? e.slice() : e, void (M = 0);if ((o = \"number\" == typeof e) && 0 * e == 0) {\n          if (f.s = 0 > 1 / e ? (e = -e, -1) : 1, e === ~~e) {\n            for (r = 0, i = e; i >= 10; i /= 10, r++) {}return f.e = r, f.c = [e], void (M = 0);\n          }s = e + \"\";\n        } else {\n          if (!p.test(s = e + \"\")) return g(f, s, o);f.s = 45 === s.charCodeAt(0) ? (s = s.slice(1), -1) : 1;\n        }\n      }for ((r = s.indexOf(\".\")) > -1 && (s = s.replace(\".\", \"\")), (i = s.search(/e/i)) > 0 ? (0 > r && (r = i), r += +s.slice(i + 1), s = s.substring(0, i)) : 0 > r && (r = s.length), i = 0; 48 === s.charCodeAt(i); i++) {}for (u = s.length; 48 === s.charCodeAt(--u);) {}if (s = s.slice(i, u + 1)) {\n        if (u = s.length, o && j && u > 15 && L(M, b, f.s * e), r = r - i - 1, r > z) f.c = f.e = null;else if (G > r) f.c = [f.e = 0];else {\n          if (f.e = r, f.c = [], i = (r + 1) % y, 0 > r && (i += y), u > i) {\n            for (i && f.c.push(+s.slice(0, i)), u -= y; u > i;) {\n              f.c.push(+s.slice(i, i += y));\n            }s = s.slice(i), i = y - s.length;\n          } else i -= u;for (; i--; s += \"0\") {}f.c.push(+s);\n        }\n      } else f.c = [f.e = 0];M = 0;\n    }function D(e, n, t, i) {\n      var o,\n          u,\n          f,\n          c,\n          h,\n          g,\n          p,\n          d = e.indexOf(\".\"),\n          m = P,\n          w = k;for (37 > t && (e = e.toLowerCase()), d >= 0 && (f = J, J = 0, e = e.replace(\".\", \"\"), p = new a(t), h = p.pow(e.length - d), J = f, p.c = s(l(r(h.c), h.e), 10, n), p.e = p.c.length), g = s(e, t, n), u = f = g.length; 0 == g[--f]; g.pop()) {}if (!g[0]) return \"0\";if (0 > d ? --u : (h.c = g, h.e = u, h.s = i, h = C(h, p, m, w, n), g = h.c, c = h.r, u = h.e), o = u + m + 1, d = g[o], f = n / 2, c = c || 0 > o || null != g[o + 1], c = 4 > w ? (null != d || c) && (0 == w || w == (h.s < 0 ? 3 : 2)) : d > f || d == f && (4 == w || c || 6 == w && 1 & g[o - 1] || w == (h.s < 0 ? 8 : 7)), 1 > o || !g[0]) e = c ? l(\"1\", -m) : \"0\";else {\n        if (g.length = o, c) for (--n; ++g[--o] > n;) {\n          g[o] = 0, o || (++u, g.unshift(1));\n        }for (f = g.length; !g[--f];) {}for (d = 0, e = \"\"; f >= d; e += O.charAt(g[d++])) {}e = l(e, u);\n      }return e;\n    }function _(e, n, t, i) {\n      var o, u, s, c, h;if (t = null != t && H(t, 0, 8, i, v) ? 0 | t : k, !e.c) return e.toString();if (o = e.c[0], s = e.e, null == n) h = r(e.c), h = 19 == i || 24 == i && B >= s ? f(h, s) : l(h, s);else if (e = U(new a(e), n, t), u = e.e, h = r(e.c), c = h.length, 19 == i || 24 == i && (u >= n || B >= u)) {\n        for (; n > c; h += \"0\", c++) {}h = f(h, u);\n      } else if (n -= s, h = l(h, u), u + 1 > c) {\n        if (--n > 0) for (h += \".\"; n--; h += \"0\") {}\n      } else if (n += u - c, n > 0) for (u + 1 == c && (h += \".\"); n--; h += \"0\") {}return e.s < 0 && o ? \"-\" + h : h;\n    }function x(e, n) {\n      var t,\n          r,\n          i = 0;for (u(e[0]) && (e = e[0]), t = new a(e[0]); ++i < e.length;) {\n        if (r = new a(e[i]), !r.s) {\n          t = r;break;\n        }n.call(t, r) && (t = r);\n      }return t;\n    }function F(e, n, t, r, i) {\n      return (n > e || e > t || e != c(e)) && L(r, (i || \"decimal places\") + (n > e || e > t ? \" out of range\" : \" not an integer\"), e), !0;\n    }function I(e, n, t) {\n      for (var r = 1, i = n.length; !n[--i]; n.pop()) {}for (i = n[0]; i >= 10; i /= 10, r++) {}return (t = r + t * y - 1) > z ? e.c = e.e = null : G > t ? e.c = [e.e = 0] : (e.e = t, e.c = n), e;\n    }function L(e, n, t) {\n      var r = new Error([\"new BigNumber\", \"cmp\", \"config\", \"div\", \"divToInt\", \"eq\", \"gt\", \"gte\", \"lt\", \"lte\", \"minus\", \"mod\", \"plus\", \"precision\", \"random\", \"round\", \"shift\", \"times\", \"toDigits\", \"toExponential\", \"toFixed\", \"toFormat\", \"toFraction\", \"pow\", \"toPrecision\", \"toString\", \"BigNumber\"][e] + \"() \" + n + \": \" + t);throw r.name = \"BigNumber Error\", M = 0, r;\n    }function U(e, n, t, r) {\n      var i,\n          o,\n          u,\n          s,\n          f,\n          l,\n          c,\n          a = e.c,\n          h = R;if (a) {\n        e: {\n          for (i = 1, s = a[0]; s >= 10; s /= 10, i++) {}if (o = n - i, 0 > o) o += y, u = n, f = a[l = 0], c = f / h[i - u - 1] % 10 | 0;else if (l = d((o + 1) / y), l >= a.length) {\n            if (!r) break e;for (; a.length <= l; a.push(0)) {}f = c = 0, i = 1, o %= y, u = o - y + 1;\n          } else {\n            for (f = s = a[l], i = 1; s >= 10; s /= 10, i++) {}o %= y, u = o - y + i, c = 0 > u ? 0 : f / h[i - u - 1] % 10 | 0;\n          }if (r = r || 0 > n || null != a[l + 1] || (0 > u ? f : f % h[i - u - 1]), r = 4 > t ? (c || r) && (0 == t || t == (e.s < 0 ? 3 : 2)) : c > 5 || 5 == c && (4 == t || r || 6 == t && (o > 0 ? u > 0 ? f / h[i - u] : 0 : a[l - 1]) % 10 & 1 || t == (e.s < 0 ? 8 : 7)), 1 > n || !a[0]) return a.length = 0, r ? (n -= e.e + 1, a[0] = h[n % y], e.e = -n || 0) : a[0] = e.e = 0, e;if (0 == o ? (a.length = l, s = 1, l--) : (a.length = l + 1, s = h[y - o], a[l] = u > 0 ? m(f / h[i - u] % h[u]) * s : 0), r) for (;;) {\n            if (0 == l) {\n              for (o = 1, u = a[0]; u >= 10; u /= 10, o++) {}for (u = a[0] += s, s = 1; u >= 10; u /= 10, s++) {}o != s && (e.e++, a[0] == N && (a[0] = 1));break;\n            }if (a[l] += s, a[l] != N) break;a[l--] = 0, s = 1;\n          }for (o = a.length; 0 === a[--o]; a.pop()) {}\n        }e.e > z ? e.c = e.e = null : e.e < G && (e.c = [e.e = 0]);\n      }return e;\n    }var C,\n        M = 0,\n        T = a.prototype,\n        q = new a(1),\n        P = 20,\n        k = 4,\n        B = -7,\n        $ = 21,\n        G = -1e7,\n        z = 1e7,\n        j = !0,\n        H = F,\n        V = !1,\n        W = 1,\n        J = 100,\n        X = { decimalSeparator: \".\", groupSeparator: \",\", groupSize: 3, secondaryGroupSize: 0, fractionGroupSeparator: \" \", fractionGroupSize: 0 };return a.another = n, a.ROUND_UP = 0, a.ROUND_DOWN = 1, a.ROUND_CEIL = 2, a.ROUND_FLOOR = 3, a.ROUND_HALF_UP = 4, a.ROUND_HALF_DOWN = 5, a.ROUND_HALF_EVEN = 6, a.ROUND_HALF_CEIL = 7, a.ROUND_HALF_FLOOR = 8, a.EUCLID = 9, a.config = function () {\n      var e,\n          n,\n          t = 0,\n          r = {},\n          i = arguments,\n          s = i[0],\n          f = s && \"object\" == (typeof s === \"undefined\" ? \"undefined\" : _typeof(s)) ? function () {\n        return s.hasOwnProperty(n) ? null != (e = s[n]) : void 0;\n      } : function () {\n        return i.length > t ? null != (e = i[t++]) : void 0;\n      };return f(n = \"DECIMAL_PLACES\") && H(e, 0, E, 2, n) && (P = 0 | e), r[n] = P, f(n = \"ROUNDING_MODE\") && H(e, 0, 8, 2, n) && (k = 0 | e), r[n] = k, f(n = \"EXPONENTIAL_AT\") && (u(e) ? H(e[0], -E, 0, 2, n) && H(e[1], 0, E, 2, n) && (B = 0 | e[0], $ = 0 | e[1]) : H(e, -E, E, 2, n) && (B = -($ = 0 | (0 > e ? -e : e)))), r[n] = [B, $], f(n = \"RANGE\") && (u(e) ? H(e[0], -E, -1, 2, n) && H(e[1], 1, E, 2, n) && (G = 0 | e[0], z = 0 | e[1]) : H(e, -E, E, 2, n) && (0 | e ? G = -(z = 0 | (0 > e ? -e : e)) : j && L(2, n + \" cannot be zero\", e))), r[n] = [G, z], f(n = \"ERRORS\") && (e === !!e || 1 === e || 0 === e ? (M = 0, H = (j = !!e) ? F : o) : j && L(2, n + w, e)), r[n] = j, f(n = \"CRYPTO\") && (e === !!e || 1 === e || 0 === e ? (V = !(!e || !h || \"object\" != (typeof h === \"undefined\" ? \"undefined\" : _typeof(h))), e && !V && j && L(2, \"crypto unavailable\", h)) : j && L(2, n + w, e)), r[n] = V, f(n = \"MODULO_MODE\") && H(e, 0, 9, 2, n) && (W = 0 | e), r[n] = W, f(n = \"POW_PRECISION\") && H(e, 0, E, 2, n) && (J = 0 | e), r[n] = J, f(n = \"FORMAT\") && (\"object\" == (typeof e === \"undefined\" ? \"undefined\" : _typeof(e)) ? X = e : j && L(2, n + \" not an object\", e)), r[n] = X, r;\n    }, a.max = function () {\n      return x(arguments, T.lt);\n    }, a.min = function () {\n      return x(arguments, T.gt);\n    }, a.random = function () {\n      var e = 9007199254740992,\n          n = Math.random() * e & 2097151 ? function () {\n        return m(Math.random() * e);\n      } : function () {\n        return 8388608 * (1073741824 * Math.random() | 0) + (8388608 * Math.random() | 0);\n      };return function (e) {\n        var t,\n            r,\n            i,\n            o,\n            u,\n            s = 0,\n            f = [],\n            l = new a(q);if (e = null != e && H(e, 0, E, 14) ? 0 | e : P, o = d(e / y), V) if (h && h.getRandomValues) {\n          for (t = h.getRandomValues(new Uint32Array(o *= 2)); o > s;) {\n            u = 131072 * t[s] + (t[s + 1] >>> 11), u >= 9e15 ? (r = h.getRandomValues(new Uint32Array(2)), t[s] = r[0], t[s + 1] = r[1]) : (f.push(u % 1e14), s += 2);\n          }s = o / 2;\n        } else if (h && h.randomBytes) {\n          for (t = h.randomBytes(o *= 7); o > s;) {\n            u = 281474976710656 * (31 & t[s]) + 1099511627776 * t[s + 1] + 4294967296 * t[s + 2] + 16777216 * t[s + 3] + (t[s + 4] << 16) + (t[s + 5] << 8) + t[s + 6], u >= 9e15 ? h.randomBytes(7).copy(t, s) : (f.push(u % 1e14), s += 7);\n          }s = o / 7;\n        } else j && L(14, \"crypto unavailable\", h);if (!s) for (; o > s;) {\n          u = n(), 9e15 > u && (f[s++] = u % 1e14);\n        }for (o = f[--s], e %= y, o && e && (u = R[y - e], f[s] = m(o / u) * u); 0 === f[s]; f.pop(), s--) {}if (0 > s) f = [i = 0];else {\n          for (i = -1; 0 === f[0]; f.shift(), i -= y) {}for (s = 1, u = f[0]; u >= 10; u /= 10, s++) {}y > s && (i -= y - s);\n        }return l.e = i, l.c = f, l;\n      };\n    }(), C = function () {\n      function e(e, n, t) {\n        var r,\n            i,\n            o,\n            u,\n            s = 0,\n            f = e.length,\n            l = n % A,\n            c = n / A | 0;for (e = e.slice(); f--;) {\n          o = e[f] % A, u = e[f] / A | 0, r = c * o + u * l, i = l * o + r % A * A + s, s = (i / t | 0) + (r / A | 0) + c * u, e[f] = i % t;\n        }return s && e.unshift(s), e;\n      }function n(e, n, t, r) {\n        var i, o;if (t != r) o = t > r ? 1 : -1;else for (i = o = 0; t > i; i++) {\n          if (e[i] != n[i]) {\n            o = e[i] > n[i] ? 1 : -1;break;\n          }\n        }return o;\n      }function r(e, n, t, r) {\n        for (var i = 0; t--;) {\n          e[t] -= i, i = e[t] < n[t] ? 1 : 0, e[t] = i * r + e[t] - n[t];\n        }for (; !e[0] && e.length > 1; e.shift()) {}\n      }return function (i, o, u, s, f) {\n        var l,\n            c,\n            h,\n            g,\n            p,\n            d,\n            w,\n            v,\n            b,\n            O,\n            S,\n            R,\n            A,\n            E,\n            D,\n            _,\n            x,\n            F = i.s == o.s ? 1 : -1,\n            I = i.c,\n            L = o.c;if (!(I && I[0] && L && L[0])) return new a(i.s && o.s && (I ? !L || I[0] != L[0] : L) ? I && 0 == I[0] || !L ? 0 * F : F / 0 : 0 / 0);for (v = new a(F), b = v.c = [], c = i.e - o.e, F = u + c + 1, f || (f = N, c = t(i.e / y) - t(o.e / y), F = F / y | 0), h = 0; L[h] == (I[h] || 0); h++) {}if (L[h] > (I[h] || 0) && c--, 0 > F) b.push(1), g = !0;else {\n          for (E = I.length, _ = L.length, h = 0, F += 2, p = m(f / (L[0] + 1)), p > 1 && (L = e(L, p, f), I = e(I, p, f), _ = L.length, E = I.length), A = _, O = I.slice(0, _), S = O.length; _ > S; O[S++] = 0) {}x = L.slice(), x.unshift(0), D = L[0], L[1] >= f / 2 && D++;do {\n            p = 0, l = n(L, O, _, S), 0 > l ? (R = O[0], _ != S && (R = R * f + (O[1] || 0)), p = m(R / D), p > 1 ? (p >= f && (p = f - 1), d = e(L, p, f), w = d.length, S = O.length, l = n(d, O, w, S), 1 == l && (p--, r(d, w > _ ? x : L, w, f))) : (0 == p && (l = p = 1), d = L.slice()), w = d.length, S > w && d.unshift(0), r(O, d, S, f), -1 == l && (S = O.length, l = n(L, O, _, S), 1 > l && (p++, r(O, S > _ ? x : L, S, f))), S = O.length) : 0 === l && (p++, O = [0]), b[h++] = p, l && O[0] ? O[S++] = I[A] || 0 : (O = [I[A]], S = 1);\n          } while ((A++ < E || null != O[0]) && F--);g = null != O[0], b[0] || b.shift();\n        }if (f == N) {\n          for (h = 1, F = b[0]; F >= 10; F /= 10, h++) {}U(v, u + (v.e = h + c * y - 1) + 1, s, g);\n        } else v.e = c, v.r = +g;return v;\n      };\n    }(), g = function () {\n      var e = /^(-?)0([xbo])(\\w[\\w.]*$)/i,\n          n = /^([^.]+)\\.$/,\n          t = /^\\.([^.]+)$/,\n          r = /^-?(Infinity|NaN)$/,\n          i = /^\\s*\\+([\\w.])|^\\s+|\\s+$/g;return function (o, u, s, f) {\n        var l,\n            c = s ? u : u.replace(i, \"$1\");if (r.test(c)) o.s = isNaN(c) ? null : 0 > c ? -1 : 1;else {\n          if (!s && (c = c.replace(e, function (e, n, t) {\n            return l = \"x\" == (t = t.toLowerCase()) ? 16 : \"b\" == t ? 2 : 8, f && f != l ? e : n;\n          }), f && (l = f, c = c.replace(n, \"$1\").replace(t, \"0.$1\")), u != c)) return new a(c, l);j && L(M, \"not a\" + (f ? \" base \" + f : \"\") + \" number\", u), o.s = null;\n        }o.c = o.e = null, M = 0;\n      };\n    }(), T.absoluteValue = T.abs = function () {\n      var e = new a(this);return e.s < 0 && (e.s = 1), e;\n    }, T.ceil = function () {\n      return U(new a(this), this.e + 1, 2);\n    }, T.comparedTo = T.cmp = function (e, n) {\n      return M = 1, i(this, new a(e, n));\n    }, T.decimalPlaces = T.dp = function () {\n      var e,\n          n,\n          r = this.c;if (!r) return null;if (e = ((n = r.length - 1) - t(this.e / y)) * y, n = r[n]) for (; n % 10 == 0; n /= 10, e--) {}return 0 > e && (e = 0), e;\n    }, T.dividedBy = T.div = function (e, n) {\n      return M = 3, C(this, new a(e, n), P, k);\n    }, T.dividedToIntegerBy = T.divToInt = function (e, n) {\n      return M = 4, C(this, new a(e, n), 0, 1);\n    }, T.equals = T.eq = function (e, n) {\n      return M = 5, 0 === i(this, new a(e, n));\n    }, T.floor = function () {\n      return U(new a(this), this.e + 1, 3);\n    }, T.greaterThan = T.gt = function (e, n) {\n      return M = 6, i(this, new a(e, n)) > 0;\n    }, T.greaterThanOrEqualTo = T.gte = function (e, n) {\n      return M = 7, 1 === (n = i(this, new a(e, n))) || 0 === n;\n    }, T.isFinite = function () {\n      return !!this.c;\n    }, T.isInteger = T.isInt = function () {\n      return !!this.c && t(this.e / y) > this.c.length - 2;\n    }, T.isNaN = function () {\n      return !this.s;\n    }, T.isNegative = T.isNeg = function () {\n      return this.s < 0;\n    }, T.isZero = function () {\n      return !!this.c && 0 == this.c[0];\n    }, T.lessThan = T.lt = function (e, n) {\n      return M = 8, i(this, new a(e, n)) < 0;\n    }, T.lessThanOrEqualTo = T.lte = function (e, n) {\n      return M = 9, -1 === (n = i(this, new a(e, n))) || 0 === n;\n    }, T.minus = T.sub = function (e, n) {\n      var r,\n          i,\n          o,\n          u,\n          s = this,\n          f = s.s;if (M = 10, e = new a(e, n), n = e.s, !f || !n) return new a(0 / 0);if (f != n) return e.s = -n, s.plus(e);var l = s.e / y,\n          c = e.e / y,\n          h = s.c,\n          g = e.c;if (!l || !c) {\n        if (!h || !g) return h ? (e.s = -n, e) : new a(g ? s : 0 / 0);if (!h[0] || !g[0]) return g[0] ? (e.s = -n, e) : new a(h[0] ? s : 3 == k ? -0 : 0);\n      }if (l = t(l), c = t(c), h = h.slice(), f = l - c) {\n        for ((u = 0 > f) ? (f = -f, o = h) : (c = l, o = g), o.reverse(), n = f; n--; o.push(0)) {}o.reverse();\n      } else for (i = (u = (f = h.length) < (n = g.length)) ? f : n, f = n = 0; i > n; n++) {\n        if (h[n] != g[n]) {\n          u = h[n] < g[n];break;\n        }\n      }if (u && (o = h, h = g, g = o, e.s = -e.s), n = (i = g.length) - (r = h.length), n > 0) for (; n--; h[r++] = 0) {}for (n = N - 1; i > f;) {\n        if (h[--i] < g[i]) {\n          for (r = i; r && !h[--r]; h[r] = n) {}--h[r], h[i] += N;\n        }h[i] -= g[i];\n      }for (; 0 == h[0]; h.shift(), --c) {}return h[0] ? I(e, h, c) : (e.s = 3 == k ? -1 : 1, e.c = [e.e = 0], e);\n    }, T.modulo = T.mod = function (e, n) {\n      var t,\n          r,\n          i = this;return M = 11, e = new a(e, n), !i.c || !e.s || e.c && !e.c[0] ? new a(0 / 0) : !e.c || i.c && !i.c[0] ? new a(i) : (9 == W ? (r = e.s, e.s = 1, t = C(i, e, 0, 3), e.s = r, t.s *= r) : t = C(i, e, 0, W), i.minus(t.times(e)));\n    }, T.negated = T.neg = function () {\n      var e = new a(this);return e.s = -e.s || null, e;\n    }, T.plus = T.add = function (e, n) {\n      var r,\n          i = this,\n          o = i.s;if (M = 12, e = new a(e, n), n = e.s, !o || !n) return new a(0 / 0);if (o != n) return e.s = -n, i.minus(e);var u = i.e / y,\n          s = e.e / y,\n          f = i.c,\n          l = e.c;if (!u || !s) {\n        if (!f || !l) return new a(o / 0);if (!f[0] || !l[0]) return l[0] ? e : new a(f[0] ? i : 0 * o);\n      }if (u = t(u), s = t(s), f = f.slice(), o = u - s) {\n        for (o > 0 ? (s = u, r = l) : (o = -o, r = f), r.reverse(); o--; r.push(0)) {}r.reverse();\n      }for (o = f.length, n = l.length, 0 > o - n && (r = l, l = f, f = r, n = o), o = 0; n;) {\n        o = (f[--n] = f[n] + l[n] + o) / N | 0, f[n] %= N;\n      }return o && (f.unshift(o), ++s), I(e, f, s);\n    }, T.precision = T.sd = function (e) {\n      var n,\n          t,\n          r = this,\n          i = r.c;if (null != e && e !== !!e && 1 !== e && 0 !== e && (j && L(13, \"argument\" + w, e), e != !!e && (e = null)), !i) return null;if (t = i.length - 1, n = t * y + 1, t = i[t]) {\n        for (; t % 10 == 0; t /= 10, n--) {}for (t = i[0]; t >= 10; t /= 10, n++) {}\n      }return e && r.e + 1 > n && (n = r.e + 1), n;\n    }, T.round = function (e, n) {\n      var t = new a(this);return (null == e || H(e, 0, E, 15)) && U(t, ~~e + this.e + 1, null != n && H(n, 0, 8, 15, v) ? 0 | n : k), t;\n    }, T.shift = function (e) {\n      var n = this;return H(e, -S, S, 16, \"argument\") ? n.times(\"1e\" + c(e)) : new a(n.c && n.c[0] && (-S > e || e > S) ? n.s * (0 > e ? 0 : 1 / 0) : n);\n    }, T.squareRoot = T.sqrt = function () {\n      var e,\n          n,\n          i,\n          o,\n          u,\n          s = this,\n          f = s.c,\n          l = s.s,\n          c = s.e,\n          h = P + 4,\n          g = new a(\"0.5\");if (1 !== l || !f || !f[0]) return new a(!l || 0 > l && (!f || f[0]) ? 0 / 0 : f ? s : 1 / 0);if (l = Math.sqrt(+s), 0 == l || l == 1 / 0 ? (n = r(f), (n.length + c) % 2 == 0 && (n += \"0\"), l = Math.sqrt(n), c = t((c + 1) / 2) - (0 > c || c % 2), l == 1 / 0 ? n = \"1e\" + c : (n = l.toExponential(), n = n.slice(0, n.indexOf(\"e\") + 1) + c), i = new a(n)) : i = new a(l + \"\"), i.c[0]) for (c = i.e, l = c + h, 3 > l && (l = 0);;) {\n        if (u = i, i = g.times(u.plus(C(s, u, h, 1))), r(u.c).slice(0, l) === (n = r(i.c)).slice(0, l)) {\n          if (i.e < c && --l, n = n.slice(l - 3, l + 1), \"9999\" != n && (o || \"4999\" != n)) {\n            (!+n || !+n.slice(1) && \"5\" == n.charAt(0)) && (U(i, i.e + P + 2, 1), e = !i.times(i).eq(s));break;\n          }if (!o && (U(u, u.e + P + 2, 0), u.times(u).eq(s))) {\n            i = u;break;\n          }h += 4, l += 4, o = 1;\n        }\n      }return U(i, i.e + P + 1, k, e);\n    }, T.times = T.mul = function (e, n) {\n      var r,\n          i,\n          o,\n          u,\n          s,\n          f,\n          l,\n          c,\n          h,\n          g,\n          p,\n          d,\n          m,\n          w,\n          v,\n          b = this,\n          O = b.c,\n          S = (M = 17, e = new a(e, n)).c;if (!(O && S && O[0] && S[0])) return !b.s || !e.s || O && !O[0] && !S || S && !S[0] && !O ? e.c = e.e = e.s = null : (e.s *= b.s, O && S ? (e.c = [0], e.e = 0) : e.c = e.e = null), e;for (i = t(b.e / y) + t(e.e / y), e.s *= b.s, l = O.length, g = S.length, g > l && (m = O, O = S, S = m, o = l, l = g, g = o), o = l + g, m = []; o--; m.push(0)) {}for (w = N, v = A, o = g; --o >= 0;) {\n        for (r = 0, p = S[o] % v, d = S[o] / v | 0, s = l, u = o + s; u > o;) {\n          c = O[--s] % v, h = O[s] / v | 0, f = d * c + h * p, c = p * c + f % v * v + m[u] + r, r = (c / w | 0) + (f / v | 0) + d * h, m[u--] = c % w;\n        }m[u] = r;\n      }return r ? ++i : m.shift(), I(e, m, i);\n    }, T.toDigits = function (e, n) {\n      var t = new a(this);return e = null != e && H(e, 1, E, 18, \"precision\") ? 0 | e : null, n = null != n && H(n, 0, 8, 18, v) ? 0 | n : k, e ? U(t, e, n) : t;\n    }, T.toExponential = function (e, n) {\n      return _(this, null != e && H(e, 0, E, 19) ? ~~e + 1 : null, n, 19);\n    }, T.toFixed = function (e, n) {\n      return _(this, null != e && H(e, 0, E, 20) ? ~~e + this.e + 1 : null, n, 20);\n    }, T.toFormat = function (e, n) {\n      var t = _(this, null != e && H(e, 0, E, 21) ? ~~e + this.e + 1 : null, n, 21);if (this.c) {\n        var r,\n            i = t.split(\".\"),\n            o = +X.groupSize,\n            u = +X.secondaryGroupSize,\n            s = X.groupSeparator,\n            f = i[0],\n            l = i[1],\n            c = this.s < 0,\n            a = c ? f.slice(1) : f,\n            h = a.length;if (u && (r = o, o = u, u = r, h -= r), o > 0 && h > 0) {\n          for (r = h % o || o, f = a.substr(0, r); h > r; r += o) {\n            f += s + a.substr(r, o);\n          }u > 0 && (f += s + a.slice(r)), c && (f = \"-\" + f);\n        }t = l ? f + X.decimalSeparator + ((u = +X.fractionGroupSize) ? l.replace(new RegExp(\"\\\\d{\" + u + \"}\\\\B\", \"g\"), \"$&\" + X.fractionGroupSeparator) : l) : f;\n      }return t;\n    }, T.toFraction = function (e) {\n      var n,\n          t,\n          i,\n          o,\n          u,\n          s,\n          f,\n          l,\n          c,\n          h = j,\n          g = this,\n          p = g.c,\n          d = new a(q),\n          m = t = new a(q),\n          w = f = new a(q);if (null != e && (j = !1, s = new a(e), j = h, (!(h = s.isInt()) || s.lt(q)) && (j && L(22, \"max denominator \" + (h ? \"out of range\" : \"not an integer\"), e), e = !h && s.c && U(s, s.e + 1, 1).gte(q) ? s : null)), !p) return g.toString();for (c = r(p), o = d.e = c.length - g.e - 1, d.c[0] = R[(u = o % y) < 0 ? y + u : u], e = !e || s.cmp(d) > 0 ? o > 0 ? d : m : s, u = z, z = 1 / 0, s = new a(c), f.c[0] = 0; l = C(s, d, 0, 1), i = t.plus(l.times(w)), 1 != i.cmp(e);) {\n        t = w, w = i, m = f.plus(l.times(i = m)), f = i, d = s.minus(l.times(i = d)), s = i;\n      }return i = C(e.minus(t), w, 0, 1), f = f.plus(i.times(m)), t = t.plus(i.times(w)), f.s = m.s = g.s, o *= 2, n = C(m, w, o, k).minus(g).abs().cmp(C(f, t, o, k).minus(g).abs()) < 1 ? [m.toString(), w.toString()] : [f.toString(), t.toString()], z = u, n;\n    }, T.toNumber = function () {\n      var e = this;return +e || (e.s ? 0 * e.s : 0 / 0);\n    }, T.toPower = T.pow = function (e) {\n      var n,\n          t,\n          r = m(0 > e ? -e : +e),\n          i = this;if (!H(e, -S, S, 23, \"exponent\") && (!isFinite(e) || r > S && (e /= 0) || parseFloat(e) != e && !(e = 0 / 0))) return new a(Math.pow(+i, e));for (n = J ? d(J / y + 2) : 0, t = new a(q);;) {\n        if (r % 2) {\n          if (t = t.times(i), !t.c) break;n && t.c.length > n && (t.c.length = n);\n        }if (r = m(r / 2), !r) break;i = i.times(i), n && i.c && i.c.length > n && (i.c.length = n);\n      }return 0 > e && (t = q.div(t)), n ? U(t, J, k) : t;\n    }, T.toPrecision = function (e, n) {\n      return _(this, null != e && H(e, 1, E, 24, \"precision\") ? 0 | e : null, n, 24);\n    }, T.toString = function (e) {\n      var n,\n          t = this,\n          i = t.s,\n          o = t.e;return null === o ? i ? (n = \"Infinity\", 0 > i && (n = \"-\" + n)) : n = \"NaN\" : (n = r(t.c), n = null != e && H(e, 2, 64, 25, \"base\") ? D(l(n, o), 0 | e, 10, i) : B >= o || o >= $ ? f(n, o) : l(n, o), 0 > i && t.c[0] && (n = \"-\" + n)), n;\n    }, T.truncated = T.trunc = function () {\n      return U(new a(this), this.e + 1, 1);\n    }, T.valueOf = T.toJSON = function () {\n      return this.toString();\n    }, null != e && a.config(e), a;\n  }function t(e) {\n    var n = 0 | e;return e > 0 || e === n ? n : n - 1;\n  }function r(e) {\n    for (var n, t, r = 1, i = e.length, o = e[0] + \"\"; i > r;) {\n      for (n = e[r++] + \"\", t = y - n.length; t--; n = \"0\" + n) {}o += n;\n    }for (i = o.length; 48 === o.charCodeAt(--i);) {}return o.slice(0, i + 1 || 1);\n  }function i(e, n) {\n    var t,\n        r,\n        i = e.c,\n        o = n.c,\n        u = e.s,\n        s = n.s,\n        f = e.e,\n        l = n.e;if (!u || !s) return null;if (t = i && !i[0], r = o && !o[0], t || r) return t ? r ? 0 : -s : u;if (u != s) return u;if (t = 0 > u, r = f == l, !i || !o) return r ? 0 : !i ^ t ? 1 : -1;if (!r) return f > l ^ t ? 1 : -1;for (s = (f = i.length) < (l = o.length) ? f : l, u = 0; s > u; u++) {\n      if (i[u] != o[u]) return i[u] > o[u] ^ t ? 1 : -1;\n    }return f == l ? 0 : f > l ^ t ? 1 : -1;\n  }function o(e, n, t) {\n    return (e = c(e)) >= n && t >= e;\n  }function u(e) {\n    return \"[object Array]\" == Object.prototype.toString.call(e);\n  }function s(e, n, t) {\n    for (var r, i, o = [0], u = 0, s = e.length; s > u;) {\n      for (i = o.length; i--; o[i] *= n) {}for (o[r = 0] += O.indexOf(e.charAt(u++)); r < o.length; r++) {\n        o[r] > t - 1 && (null == o[r + 1] && (o[r + 1] = 0), o[r + 1] += o[r] / t | 0, o[r] %= t);\n      }\n    }return o.reverse();\n  }function f(e, n) {\n    return (e.length > 1 ? e.charAt(0) + \".\" + e.slice(1) : e) + (0 > n ? \"e\" : \"e+\") + n;\n  }function l(e, n) {\n    var t, r;if (0 > n) {\n      for (r = \"0.\"; ++n; r += \"0\") {}e = r + e;\n    } else if (t = e.length, ++n > t) {\n      for (r = \"0\", n -= t; --n; r += \"0\") {}e += r;\n    } else t > n && (e = e.slice(0, n) + \".\" + e.slice(n));return e;\n  }function c(e) {\n    return e = parseFloat(e), 0 > e ? d(e) : m(e);\n  }var a,\n      h,\n      g,\n      p = /^-?(\\d+(\\.\\d*)?|\\.\\d+)(e[+-]?\\d+)?$/i,\n      d = Math.ceil,\n      m = Math.floor,\n      w = \" not a boolean or binary digit\",\n      v = \"rounding mode\",\n      b = \"number type has more than 15 significant digits\",\n      O = \"0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ$_\",\n      N = 1e14,\n      y = 14,\n      S = 9007199254740991,\n      R = [1, 10, 100, 1e3, 1e4, 1e5, 1e6, 1e7, 1e8, 1e9, 1e10, 1e11, 1e12, 1e13],\n      A = 1e7,\n      E = 1e9;if (a = n(), \"function\" == \"function\" && __webpack_require__(/*! !webpack amd options */ \"./node_modules/webpack/buildin/amd-options.js\")) !(__WEBPACK_AMD_DEFINE_RESULT__ = (function () {\n    return a;\n  }).call(exports, __webpack_require__, exports, module),\n\t\t\t\t__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));else if (\"undefined\" != typeof module && module.exports) {\n    if (module.exports = a, !h) try {\n      h = __webpack_require__(!(function webpackMissingModule() { var e = new Error(\"Cannot find module ''\"); e.code = 'MODULE_NOT_FOUND'; throw e; }()));\n    } catch (D) {}\n  } else e.BigNumber = a;\n}(undefined);\n//# sourceMappingURL=doc/bignumber.js.map\n\n//# sourceURL=webpack://Penta/./lib/plugins/bignumber.js?");

/***/ }),

/***/ "./lib/pollingManager.js":
/*!*******************************!*\
  !*** ./lib/pollingManager.js ***!
  \*******************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\n/**\n * PollingManager module\n * @description call the same callback function every poll，cases：filter, asyncing\n * @class PollingManager\n * @author Buddywangw <buddywangw@gmail.com>\n */\n\nvar config = __webpack_require__(/*! ./config */ \"./lib/config.js\");\n\nvar PollingManager = function () {\n  function PollingManager(penta) {\n    _classCallCheck(this, PollingManager);\n\n    this._request = penta._request;\n    this.timeUnit = config.POLLING_TIMEOUT || 500;\n    this.polls = {};\n    this.pollData = {};\n    this.callback = null;\n    this.timeoutId = null;\n  }\n\n  /**\n   * @param {object} payload -  request data\n   * @param {function} callback\n   * @param {function} uninstall - distory method\n   * @memberof PollingManager\n   */\n\n\n  _createClass(PollingManager, [{\n    key: \"startSync\",\n    value: function startSync(payload, callback, uninstall) {\n      this.callback = callback;\n\n      this.polls[payload.id] = {\n        payload: payload,\n        id: payload.id,\n        callback: callback,\n        uninstall: uninstall\n      };\n\n      // start polling\n      if (!this.timeoutId) {\n        return this.start();\n      }\n    }\n  }, {\n    key: \"start\",\n    value: function start() {\n      var _this = this;\n\n      this.timeoutId = setTimeout(function () {\n        _this.start();\n      }, this.timeUnit);\n\n      if (Object.keys(this.polls).length === 0) {\n        return;\n      }\n\n      var payloads = [];\n\n      for (var key in this.polls) {\n        payloads.push(this.polls[key].payload);\n      }\n\n      return this._request.sendAsync(payloads, function (results) {\n        results.map(function (result) {\n          var id = result.id;\n          if (result.error) {\n            _this.polls[id].callback({\n              code: \"error\",\n              message: result.error.message\n            });\n          } else {\n            _this.polls[id].callback(result.result);\n          }\n        });\n      });\n    }\n  }, {\n    key: \"stop\",\n    value: function stop(pollId) {\n      delete this.polls[pollId];\n      if (Object.keys(this.polls).length === 0 && this.timeoutId) {\n        clearTimeout(this.timeoutId);\n        this.timeoutId = null;\n      }\n    }\n  }, {\n    key: \"reset\",\n    value: function reset() {\n      for (var key in this.polls) {\n        this.polls[key].uninstall();\n        delete this.polls[key];\n      }\n      clearTimeout(this.timeoutId);\n      this.timeoutId = null;\n    }\n  }]);\n\n  return PollingManager;\n}();\n\nmodule.exports = PollingManager;\n\n//# sourceURL=webpack://Penta/./lib/pollingManager.js?");

/***/ }),

/***/ "./lib/utils/index.js":
/*!****************************!*\
  !*** ./lib/utils/index.js ***!
  \****************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _typeof = typeof Symbol === \"function\" && typeof Symbol.iterator === \"symbol\" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === \"function\" && obj.constructor === Symbol && obj !== Symbol.prototype ? \"symbol\" : typeof obj; };\n\n/**\n * utils\n * @author Buddywangw <buddywangw@gmail.com>\n */\nvar utf8 = __webpack_require__(/*! utf8 */ \"./node_modules/utf8/utf8.js\");\nvar BigNumber = __webpack_require__(/*! ../plugins/bignumber */ \"./lib/plugins/bignumber.js\");\nvar config = __webpack_require__(/*! ../config */ \"./lib/config.js\");\n\n// u unit\nvar unitMap = {\n  nopenta: \"0\",\n  u: \"1\",\n  ku: \"1000\",\n  Ku: \"1000\",\n  babbage: \"1000\",\n  femtopenta: \"1000\",\n  mu: \"1000000\",\n  Mu: \"1000000\",\n  lovelace: \"1000000\",\n  picopenta: \"1000000\",\n  gu: \"1000000000\",\n  Gu: \"1000000000\",\n  shannon: \"1000000000\",\n  nanopenta: \"1000000000\",\n  nano: \"1000000000\",\n  szabo: \"1000000000000\",\n  micropenta: \"1000000000000\",\n  micro: \"1000000000000\",\n  finney: \"1000000000000000\",\n  millipenta: \"1000000000000000\",\n  milli: \"1000000000000000\",\n  penta: \"1000000000000000000\",\n  kpenta: \"1000000000000000000000\",\n  grand: \"1000000000000000000000\",\n  mpenta: \"1000000000000000000000000\",\n  gpenta: \"1000000000000000000000000000\",\n  tpenta: \"1000000000000000000000000000000\"\n};\n\nvar isBrowser = function isBrowser() {\n  return typeof window !== \"undefined\";\n};\n\nvar isBigNumber = function isBigNumber(obj) {\n  return obj instanceof BigNumber || obj && obj.constructor && obj.constructor.name === \"BigNumber\";\n};\n\nvar isString = function isString(object) {\n  return typeof object === \"string\" || object && object.constructor && object.constructor.name === \"String\";\n};\n\nvar isFunction = function isFunction(object) {\n  return typeof object === \"function\";\n};\n\nvar isBoolean = function isBoolean(object) {\n  return typeof object === \"boolean\" || object && object.constructor && object.constructor.name === \"Boolean\";\n};\n\nvar toString = Object.prototype.toString;\n\nvar isArray = function isArray(val) {\n  return toString.call(val) === \"[object Array]\";\n};\n\n/**\n * Convert the given number to BigNumber\n * @param {number} number - given number\n * @returns {object} - BigNumber\n */\nvar toBigNumber = function toBigNumber(number) {\n  number = number || 0;\n  if (isBigNumber(number)) return number;\n\n  if (isString(number) && (number.indexOf(\"0x\") === 0 || number.indexOf(\"-0x\") === 0)) {\n    return new BigNumber(number.replace(\"0x\", \"\"), 16);\n  }\n\n  return new BigNumber(number.toString(10), 10);\n};\n\n//Convert the value of interface return to BigNumber\nvar outputBigNumberFormatter = function outputBigNumberFormatter(number) {\n  return toBigNumber(number);\n};\n\n/**\n * Hexadecimal to decimal\n * @param {string} value - hexadecimal number\n * @returns {number} - corresponding decimal number\n */\nvar toDecimal = function toDecimal(value) {\n  return toBigNumber(value).toNumber();\n};\n\n/**\n * decimal to hexadecimal\n * @param {number} value - decimal number\n * @return {object} - corresponding hexadecimal number\n */\nvar fromDecimal = function fromDecimal(value) {\n  var number = toBigNumber(value);\n  var result = number.toString(16);\n  //number.lessThen\n  return number < 0 ? \"-0x\" + result.substr(1) : \"0x\" + result;\n};\n\n/**\n * Determine if it is the wallet address\n * @param {string} address wallet address\n * @returns {boolean} - true:a wallet address, false: not a wallet address\n */\nvar isAddress = function isAddress(address) {\n  return (/^P[0-9a-zA-Z]{33}$/i.test(address)\n  );\n};\n\nvar getValueOfUnit = function getValueOfUnit(unit) {\n  unit = unit ? unit.toLowerCase() : \"penta\";\n  var unitValue = unitMap[unit];\n  if (unitValue === undefined) {\n    throw new Error(\"Invalid unit\");\n  }\n  return new BigNumber(unitValue, 10);\n};\n\n/**\n * currency converts to u\n * @param {string} number - currency to be converted\n * @param {string} unit - unit\n * @returns {string} - data in u format\n */\nvar toU = function toU(number, unit) {\n  var returnValue = toBigNumber(number).times(getValueOfUnit(unit));\n  return isBigNumber(number) ? returnValue : returnValue.toString(10);\n};\n\n/**\n * u converts to currency\n * @param {string} number - data in U\n * @param {string} unit - unit\n * @returns {number} - corresponding currency\n */\nvar fromU = function fromU(number, unit) {\n  var returnValue = toBigNumber(number).dividedBy(getValueOfUnit(unit));\n  return isBigNumber(number) ? returnValue : returnValue.toString(10);\n};\n\nvar fromUtf8 = function fromUtf8(str, allowZero) {\n  str = utf8.encode(str);\n  var hex = \"\";\n  for (var i = 0; i < str.length; i++) {\n    var code = str.charCodeAt(i);\n    if (code === 0) {\n      if (allowZero) {\n        hex += \"00\";\n      } else {\n        break;\n      }\n    } else {\n      var n = code.toString(16);\n      hex += n.length < 2 ? \"0\" + n : n;\n    }\n  }\n\n  return \"0x\" + hex;\n};\n\n/**\n * Converts the value to hexadecimal\n * @param {string} val - the value to be converted\n * @returns {string} corresponding HEX\n */\nvar toHex = function toHex(val) {\n  if (isBoolean(val)) return fromDecimal(+val);\n  if (isBigNumber(val)) return fromDecimal(val);\n  if ((typeof val === \"undefined\" ? \"undefined\" : _typeof(val)) === \"object\") return fromUtf8(JSON.stringify(val));\n  if (isString(val)) {\n    if (val.indexOf(\"-0x\") === 0) return fromDecimal(val);else if (val.indexOf(\"0x\") === 0) return val;else if (!isFinite(val)) return fromUtf8(val, 1);\n  }\n\n  return fromDecimal(val);\n};\n\n/**\n * Converts hexadecimal to ASCII3 string\n * @param {string} hex - the value to be converted\n * @returns {string} corresponding ASCII3 string\n */\nvar toAscii = function toAscii(hex) {\n  var str = \"\";\n  var i = 0,\n      l = hex.length;\n  if (hex.substring(0, 2) === \"0x\") {\n    i = 2;\n  }\n  for (; i < l; i += 2) {\n    var code = parseInt(hex.substr(i, 2), 16);\n    str += String.fromCharCode(code);\n  }\n  return str;\n};\n\n/**\n * Converts an ASCII string to an HEX(l6 base) string.\n * @param {string} str - ASCII string to be converted\n * @param {number} num - the size of the returned string is not long enough to be automatically filled.\n * @returns {string} hexadecimal string\n */\nvar fromAscii = function fromAscii(str, num) {\n  var hex = \"\";\n  for (var i = 0; i < str.length; i++) {\n    var code = str.charCodeAt(i);\n    var n = code.toString(16);\n    hex += n.length < 2 ? \"0\" + n : n;\n  }\n  return \"0x\" + hex.padEnd(num, \"0\");\n};\n\n//Format the transaction receipt\nvar outputTransactionReceiptFormatter = function outputTransactionReceiptFormatter(receipt) {\n  if (receipt.blockNumber !== null) receipt.blockNumber = toDecimal(receipt.blockNumber);\n  if (receipt.transactionIndex !== null) receipt.transactionIndex = toDecimal(receipt.transactionIndex);\n  receipt.cumulativeGasUsed = toDecimal(receipt.cumulativeGasUsed);\n  receipt.gasUsed = toDecimal(receipt.gasUsed);\n  return receipt;\n};\n\nvar inputBlockNumberFormatter = function inputBlockNumberFormatter(blockNumber) {\n  if (blockNumber === undefined) {\n    return config.defaultBlock;\n  } else if (blockNumber === \"latest\" || blockNumber === \"pending\" || blockNumber === \"earliest\") {\n    return blockNumber;\n  }\n  return toHex(blockNumber);\n};\n\nmodule.exports = {\n  BigNumber: BigNumber,\n  isArray: isArray,\n  isBrowser: isBrowser,\n  isBigNumber: isBigNumber,\n  isString: isString,\n  isBoolean: isBoolean,\n  toHex: toHex,\n  toAscii: toAscii,\n  fromAscii: fromAscii,\n  toBigNumber: toBigNumber,\n  inputBlockNumberFormatter: inputBlockNumberFormatter,\n  outputBigNumberFormatter: outputBigNumberFormatter,\n  outputTransactionReceiptFormatter: outputTransactionReceiptFormatter,\n  toDecimal: toDecimal,\n  fromDecimal: fromDecimal,\n  toU: toU,\n  fromU: fromU,\n  isAddress: isAddress,\n  isFunction: isFunction\n};\n\n//# sourceURL=webpack://Penta/./lib/utils/index.js?");

/***/ }),

/***/ "./lib/version.json":
/*!**************************!*\
  !*** ./lib/version.json ***!
  \**************************/
/*! exports provided: version, default */
/***/ (function(module) {

eval("module.exports = {\"version\":\"0.3.0-alpha\"};\n\n//# sourceURL=webpack://Penta/./lib/version.json?");

/***/ }),

/***/ "./lib/websocketRequest.js":
/*!*********************************!*\
  !*** ./lib/websocketRequest.js ***!
  \*********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nvar Jsonrpc = __webpack_require__(/*! ./jsonrpc */ \"./lib/jsonrpc.js\");\n/**\n * WebsocketRequest\n * @description Only support browser calls, the supported methods：newPendingTransactions|newHeads|logs\n * @class WebsocketRequest\n * @author Buddywangw <buddywangw@gmail.com>\n */\n\nvar WebsocketRequest = function () {\n  function WebsocketRequest(socketUrl, onOpen) {\n    _classCallCheck(this, WebsocketRequest);\n\n    this.onOpen = onOpen;\n    this.socket = new WebSocket(socketUrl); // eslint-disable-line\n    //subscription id\n    this.subId = \"\";\n    this._bindEvent();\n  }\n\n  _createClass(WebsocketRequest, [{\n    key: \"_bindEvent\",\n    value: function _bindEvent() {\n      var _this = this;\n\n      this.socket.onopen = function () {\n        _this.onOpen();\n      };\n\n      this.socket.onmessage = function (result) {\n        var data = JSON.parse(result.data);\n        if (data.params) {\n          _this.responseCallback(data.params.result);\n        }\n        if (data.id) {\n          //subscribe success\n          if (data.id === _this.subRequestId) {\n            _this.subId = data.result;\n          }\n\n          //cancel subscribe\n          if (data.id === _this.unsubRequestId && data.result) {\n            if (_this.unSubCallback) {\n              _this.unSubCallback();\n            }\n          }\n        }\n      };\n\n      this.socket.onclose = function () {\n        console.log(\"WebSocket connect is closed\");\n      };\n\n      this.socket.nerror = function (err) {\n        console.error(\"WebSocket connect error: \" + err.message);\n      };\n    }\n  }, {\n    key: \"watch\",\n    value: function watch(params, callback) {\n      var payload = Jsonrpc.toPayload(\"filter_subscribe\", params);\n      this.socket.send(JSON.stringify(payload));\n      this.responseCallback = callback;\n      this.subRequestId = payload.id;\n      return this;\n    }\n  }, {\n    key: \"stop\",\n    value: function stop(callback) {\n      if (this.subId) {\n        var payload = Jsonrpc.toPayload(\"filter_unsubscribe\", this.subId);\n        this.socket.send(JSON.stringify(payload));\n        this.unSubCallback = callback;\n        this.unsubRequestId = payload.id;\n      }\n    }\n  }]);\n\n  return WebsocketRequest;\n}();\n\nmodule.exports = WebsocketRequest;\n\n//# sourceURL=webpack://Penta/./lib/websocketRequest.js?");

/***/ }),

/***/ "./node_modules/axios/index.js":
/*!*************************************!*\
  !*** ./node_modules/axios/index.js ***!
  \*************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("module.exports = __webpack_require__(/*! ./lib/axios */ \"./node_modules/axios/lib/axios.js\");\n\n//# sourceURL=webpack://Penta/./node_modules/axios/index.js?");

/***/ }),

/***/ "./node_modules/axios/lib/adapters/xhr.js":
/*!************************************************!*\
  !*** ./node_modules/axios/lib/adapters/xhr.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ./../utils */ \"./node_modules/axios/lib/utils.js\");\nvar settle = __webpack_require__(/*! ./../core/settle */ \"./node_modules/axios/lib/core/settle.js\");\nvar buildURL = __webpack_require__(/*! ./../helpers/buildURL */ \"./node_modules/axios/lib/helpers/buildURL.js\");\nvar parseHeaders = __webpack_require__(/*! ./../helpers/parseHeaders */ \"./node_modules/axios/lib/helpers/parseHeaders.js\");\nvar isURLSameOrigin = __webpack_require__(/*! ./../helpers/isURLSameOrigin */ \"./node_modules/axios/lib/helpers/isURLSameOrigin.js\");\nvar createError = __webpack_require__(/*! ../core/createError */ \"./node_modules/axios/lib/core/createError.js\");\nvar btoa = (typeof window !== 'undefined' && window.btoa && window.btoa.bind(window)) || __webpack_require__(/*! ./../helpers/btoa */ \"./node_modules/axios/lib/helpers/btoa.js\");\n\nmodule.exports = function xhrAdapter(config) {\n  return new Promise(function dispatchXhrRequest(resolve, reject) {\n    var requestData = config.data;\n    var requestHeaders = config.headers;\n\n    if (utils.isFormData(requestData)) {\n      delete requestHeaders['Content-Type']; // Let the browser set it\n    }\n\n    var request = new XMLHttpRequest();\n    var loadEvent = 'onreadystatechange';\n    var xDomain = false;\n\n    // For IE 8/9 CORS support\n    // Only supports POST and GET calls and doesn't returns the response headers.\n    // DON'T do this for testing b/c XMLHttpRequest is mocked, not XDomainRequest.\n    if (undefined !== 'test' &&\n        typeof window !== 'undefined' &&\n        window.XDomainRequest && !('withCredentials' in request) &&\n        !isURLSameOrigin(config.url)) {\n      request = new window.XDomainRequest();\n      loadEvent = 'onload';\n      xDomain = true;\n      request.onprogress = function handleProgress() {};\n      request.ontimeout = function handleTimeout() {};\n    }\n\n    // HTTP basic authentication\n    if (config.auth) {\n      var username = config.auth.username || '';\n      var password = config.auth.password || '';\n      requestHeaders.Authorization = 'Basic ' + btoa(username + ':' + password);\n    }\n\n    request.open(config.method.toUpperCase(), buildURL(config.url, config.params, config.paramsSerializer), true);\n\n    // Set the request timeout in MS\n    request.timeout = config.timeout;\n\n    // Listen for ready state\n    request[loadEvent] = function handleLoad() {\n      if (!request || (request.readyState !== 4 && !xDomain)) {\n        return;\n      }\n\n      // The request errored out and we didn't get a response, this will be\n      // handled by onerror instead\n      // With one exception: request that using file: protocol, most browsers\n      // will return status as 0 even though it's a successful request\n      if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf('file:') === 0)) {\n        return;\n      }\n\n      // Prepare the response\n      var responseHeaders = 'getAllResponseHeaders' in request ? parseHeaders(request.getAllResponseHeaders()) : null;\n      var responseData = !config.responseType || config.responseType === 'text' ? request.responseText : request.response;\n      var response = {\n        data: responseData,\n        // IE sends 1223 instead of 204 (https://github.com/axios/axios/issues/201)\n        status: request.status === 1223 ? 204 : request.status,\n        statusText: request.status === 1223 ? 'No Content' : request.statusText,\n        headers: responseHeaders,\n        config: config,\n        request: request\n      };\n\n      settle(resolve, reject, response);\n\n      // Clean up request\n      request = null;\n    };\n\n    // Handle low level network errors\n    request.onerror = function handleError() {\n      // Real errors are hidden from us by the browser\n      // onerror should only fire if it's a network error\n      reject(createError('Network Error', config, null, request));\n\n      // Clean up request\n      request = null;\n    };\n\n    // Handle timeout\n    request.ontimeout = function handleTimeout() {\n      reject(createError('timeout of ' + config.timeout + 'ms exceeded', config, 'ECONNABORTED',\n        request));\n\n      // Clean up request\n      request = null;\n    };\n\n    // Add xsrf header\n    // This is only done if running in a standard browser environment.\n    // Specifically not if we're in a web worker, or react-native.\n    if (utils.isStandardBrowserEnv()) {\n      var cookies = __webpack_require__(/*! ./../helpers/cookies */ \"./node_modules/axios/lib/helpers/cookies.js\");\n\n      // Add xsrf header\n      var xsrfValue = (config.withCredentials || isURLSameOrigin(config.url)) && config.xsrfCookieName ?\n          cookies.read(config.xsrfCookieName) :\n          undefined;\n\n      if (xsrfValue) {\n        requestHeaders[config.xsrfHeaderName] = xsrfValue;\n      }\n    }\n\n    // Add headers to the request\n    if ('setRequestHeader' in request) {\n      utils.forEach(requestHeaders, function setRequestHeader(val, key) {\n        if (typeof requestData === 'undefined' && key.toLowerCase() === 'content-type') {\n          // Remove Content-Type if data is undefined\n          delete requestHeaders[key];\n        } else {\n          // Otherwise add header to the request\n          request.setRequestHeader(key, val);\n        }\n      });\n    }\n\n    // Add withCredentials to request if needed\n    if (config.withCredentials) {\n      request.withCredentials = true;\n    }\n\n    // Add responseType to request if needed\n    if (config.responseType) {\n      try {\n        request.responseType = config.responseType;\n      } catch (e) {\n        // Expected DOMException thrown by browsers not compatible XMLHttpRequest Level 2.\n        // But, this can be suppressed for 'json' type as it can be parsed by default 'transformResponse' function.\n        if (config.responseType !== 'json') {\n          throw e;\n        }\n      }\n    }\n\n    // Handle progress if needed\n    if (typeof config.onDownloadProgress === 'function') {\n      request.addEventListener('progress', config.onDownloadProgress);\n    }\n\n    // Not all browsers support upload events\n    if (typeof config.onUploadProgress === 'function' && request.upload) {\n      request.upload.addEventListener('progress', config.onUploadProgress);\n    }\n\n    if (config.cancelToken) {\n      // Handle cancellation\n      config.cancelToken.promise.then(function onCanceled(cancel) {\n        if (!request) {\n          return;\n        }\n\n        request.abort();\n        reject(cancel);\n        // Clean up request\n        request = null;\n      });\n    }\n\n    if (requestData === undefined) {\n      requestData = null;\n    }\n\n    // Send the request\n    request.send(requestData);\n  });\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/adapters/xhr.js?");

/***/ }),

/***/ "./node_modules/axios/lib/axios.js":
/*!*****************************************!*\
  !*** ./node_modules/axios/lib/axios.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ./utils */ \"./node_modules/axios/lib/utils.js\");\nvar bind = __webpack_require__(/*! ./helpers/bind */ \"./node_modules/axios/lib/helpers/bind.js\");\nvar Axios = __webpack_require__(/*! ./core/Axios */ \"./node_modules/axios/lib/core/Axios.js\");\nvar defaults = __webpack_require__(/*! ./defaults */ \"./node_modules/axios/lib/defaults.js\");\n\n/**\n * Create an instance of Axios\n *\n * @param {Object} defaultConfig The default config for the instance\n * @return {Axios} A new instance of Axios\n */\nfunction createInstance(defaultConfig) {\n  var context = new Axios(defaultConfig);\n  var instance = bind(Axios.prototype.request, context);\n\n  // Copy axios.prototype to instance\n  utils.extend(instance, Axios.prototype, context);\n\n  // Copy context to instance\n  utils.extend(instance, context);\n\n  return instance;\n}\n\n// Create the default instance to be exported\nvar axios = createInstance(defaults);\n\n// Expose Axios class to allow class inheritance\naxios.Axios = Axios;\n\n// Factory for creating new instances\naxios.create = function create(instanceConfig) {\n  return createInstance(utils.merge(defaults, instanceConfig));\n};\n\n// Expose Cancel & CancelToken\naxios.Cancel = __webpack_require__(/*! ./cancel/Cancel */ \"./node_modules/axios/lib/cancel/Cancel.js\");\naxios.CancelToken = __webpack_require__(/*! ./cancel/CancelToken */ \"./node_modules/axios/lib/cancel/CancelToken.js\");\naxios.isCancel = __webpack_require__(/*! ./cancel/isCancel */ \"./node_modules/axios/lib/cancel/isCancel.js\");\n\n// Expose all/spread\naxios.all = function all(promises) {\n  return Promise.all(promises);\n};\naxios.spread = __webpack_require__(/*! ./helpers/spread */ \"./node_modules/axios/lib/helpers/spread.js\");\n\nmodule.exports = axios;\n\n// Allow use of default import syntax in TypeScript\nmodule.exports.default = axios;\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/axios.js?");

/***/ }),

/***/ "./node_modules/axios/lib/cancel/Cancel.js":
/*!*************************************************!*\
  !*** ./node_modules/axios/lib/cancel/Cancel.js ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\n/**\n * A `Cancel` is an object that is thrown when an operation is canceled.\n *\n * @class\n * @param {string=} message The message.\n */\nfunction Cancel(message) {\n  this.message = message;\n}\n\nCancel.prototype.toString = function toString() {\n  return 'Cancel' + (this.message ? ': ' + this.message : '');\n};\n\nCancel.prototype.__CANCEL__ = true;\n\nmodule.exports = Cancel;\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/cancel/Cancel.js?");

/***/ }),

/***/ "./node_modules/axios/lib/cancel/CancelToken.js":
/*!******************************************************!*\
  !*** ./node_modules/axios/lib/cancel/CancelToken.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar Cancel = __webpack_require__(/*! ./Cancel */ \"./node_modules/axios/lib/cancel/Cancel.js\");\n\n/**\n * A `CancelToken` is an object that can be used to request cancellation of an operation.\n *\n * @class\n * @param {Function} executor The executor function.\n */\nfunction CancelToken(executor) {\n  if (typeof executor !== 'function') {\n    throw new TypeError('executor must be a function.');\n  }\n\n  var resolvePromise;\n  this.promise = new Promise(function promiseExecutor(resolve) {\n    resolvePromise = resolve;\n  });\n\n  var token = this;\n  executor(function cancel(message) {\n    if (token.reason) {\n      // Cancellation has already been requested\n      return;\n    }\n\n    token.reason = new Cancel(message);\n    resolvePromise(token.reason);\n  });\n}\n\n/**\n * Throws a `Cancel` if cancellation has been requested.\n */\nCancelToken.prototype.throwIfRequested = function throwIfRequested() {\n  if (this.reason) {\n    throw this.reason;\n  }\n};\n\n/**\n * Returns an object that contains a new `CancelToken` and a function that, when called,\n * cancels the `CancelToken`.\n */\nCancelToken.source = function source() {\n  var cancel;\n  var token = new CancelToken(function executor(c) {\n    cancel = c;\n  });\n  return {\n    token: token,\n    cancel: cancel\n  };\n};\n\nmodule.exports = CancelToken;\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/cancel/CancelToken.js?");

/***/ }),

/***/ "./node_modules/axios/lib/cancel/isCancel.js":
/*!***************************************************!*\
  !*** ./node_modules/axios/lib/cancel/isCancel.js ***!
  \***************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nmodule.exports = function isCancel(value) {\n  return !!(value && value.__CANCEL__);\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/cancel/isCancel.js?");

/***/ }),

/***/ "./node_modules/axios/lib/core/Axios.js":
/*!**********************************************!*\
  !*** ./node_modules/axios/lib/core/Axios.js ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar defaults = __webpack_require__(/*! ./../defaults */ \"./node_modules/axios/lib/defaults.js\");\nvar utils = __webpack_require__(/*! ./../utils */ \"./node_modules/axios/lib/utils.js\");\nvar InterceptorManager = __webpack_require__(/*! ./InterceptorManager */ \"./node_modules/axios/lib/core/InterceptorManager.js\");\nvar dispatchRequest = __webpack_require__(/*! ./dispatchRequest */ \"./node_modules/axios/lib/core/dispatchRequest.js\");\n\n/**\n * Create a new instance of Axios\n *\n * @param {Object} instanceConfig The default config for the instance\n */\nfunction Axios(instanceConfig) {\n  this.defaults = instanceConfig;\n  this.interceptors = {\n    request: new InterceptorManager(),\n    response: new InterceptorManager()\n  };\n}\n\n/**\n * Dispatch a request\n *\n * @param {Object} config The config specific for this request (merged with this.defaults)\n */\nAxios.prototype.request = function request(config) {\n  /*eslint no-param-reassign:0*/\n  // Allow for axios('example/url'[, config]) a la fetch API\n  if (typeof config === 'string') {\n    config = utils.merge({\n      url: arguments[0]\n    }, arguments[1]);\n  }\n\n  config = utils.merge(defaults, {method: 'get'}, this.defaults, config);\n  config.method = config.method.toLowerCase();\n\n  // Hook up interceptors middleware\n  var chain = [dispatchRequest, undefined];\n  var promise = Promise.resolve(config);\n\n  this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {\n    chain.unshift(interceptor.fulfilled, interceptor.rejected);\n  });\n\n  this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {\n    chain.push(interceptor.fulfilled, interceptor.rejected);\n  });\n\n  while (chain.length) {\n    promise = promise.then(chain.shift(), chain.shift());\n  }\n\n  return promise;\n};\n\n// Provide aliases for supported request methods\nutils.forEach(['delete', 'get', 'head', 'options'], function forEachMethodNoData(method) {\n  /*eslint func-names:0*/\n  Axios.prototype[method] = function(url, config) {\n    return this.request(utils.merge(config || {}, {\n      method: method,\n      url: url\n    }));\n  };\n});\n\nutils.forEach(['post', 'put', 'patch'], function forEachMethodWithData(method) {\n  /*eslint func-names:0*/\n  Axios.prototype[method] = function(url, data, config) {\n    return this.request(utils.merge(config || {}, {\n      method: method,\n      url: url,\n      data: data\n    }));\n  };\n});\n\nmodule.exports = Axios;\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/core/Axios.js?");

/***/ }),

/***/ "./node_modules/axios/lib/core/InterceptorManager.js":
/*!***********************************************************!*\
  !*** ./node_modules/axios/lib/core/InterceptorManager.js ***!
  \***********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ./../utils */ \"./node_modules/axios/lib/utils.js\");\n\nfunction InterceptorManager() {\n  this.handlers = [];\n}\n\n/**\n * Add a new interceptor to the stack\n *\n * @param {Function} fulfilled The function to handle `then` for a `Promise`\n * @param {Function} rejected The function to handle `reject` for a `Promise`\n *\n * @return {Number} An ID used to remove interceptor later\n */\nInterceptorManager.prototype.use = function use(fulfilled, rejected) {\n  this.handlers.push({\n    fulfilled: fulfilled,\n    rejected: rejected\n  });\n  return this.handlers.length - 1;\n};\n\n/**\n * Remove an interceptor from the stack\n *\n * @param {Number} id The ID that was returned by `use`\n */\nInterceptorManager.prototype.eject = function eject(id) {\n  if (this.handlers[id]) {\n    this.handlers[id] = null;\n  }\n};\n\n/**\n * Iterate over all the registered interceptors\n *\n * This method is particularly useful for skipping over any\n * interceptors that may have become `null` calling `eject`.\n *\n * @param {Function} fn The function to call for each interceptor\n */\nInterceptorManager.prototype.forEach = function forEach(fn) {\n  utils.forEach(this.handlers, function forEachHandler(h) {\n    if (h !== null) {\n      fn(h);\n    }\n  });\n};\n\nmodule.exports = InterceptorManager;\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/core/InterceptorManager.js?");

/***/ }),

/***/ "./node_modules/axios/lib/core/createError.js":
/*!****************************************************!*\
  !*** ./node_modules/axios/lib/core/createError.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar enhanceError = __webpack_require__(/*! ./enhanceError */ \"./node_modules/axios/lib/core/enhanceError.js\");\n\n/**\n * Create an Error with the specified message, config, error code, request and response.\n *\n * @param {string} message The error message.\n * @param {Object} config The config.\n * @param {string} [code] The error code (for example, 'ECONNABORTED').\n * @param {Object} [request] The request.\n * @param {Object} [response] The response.\n * @returns {Error} The created error.\n */\nmodule.exports = function createError(message, config, code, request, response) {\n  var error = new Error(message);\n  return enhanceError(error, config, code, request, response);\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/core/createError.js?");

/***/ }),

/***/ "./node_modules/axios/lib/core/dispatchRequest.js":
/*!********************************************************!*\
  !*** ./node_modules/axios/lib/core/dispatchRequest.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ./../utils */ \"./node_modules/axios/lib/utils.js\");\nvar transformData = __webpack_require__(/*! ./transformData */ \"./node_modules/axios/lib/core/transformData.js\");\nvar isCancel = __webpack_require__(/*! ../cancel/isCancel */ \"./node_modules/axios/lib/cancel/isCancel.js\");\nvar defaults = __webpack_require__(/*! ../defaults */ \"./node_modules/axios/lib/defaults.js\");\nvar isAbsoluteURL = __webpack_require__(/*! ./../helpers/isAbsoluteURL */ \"./node_modules/axios/lib/helpers/isAbsoluteURL.js\");\nvar combineURLs = __webpack_require__(/*! ./../helpers/combineURLs */ \"./node_modules/axios/lib/helpers/combineURLs.js\");\n\n/**\n * Throws a `Cancel` if cancellation has been requested.\n */\nfunction throwIfCancellationRequested(config) {\n  if (config.cancelToken) {\n    config.cancelToken.throwIfRequested();\n  }\n}\n\n/**\n * Dispatch a request to the server using the configured adapter.\n *\n * @param {object} config The config that is to be used for the request\n * @returns {Promise} The Promise to be fulfilled\n */\nmodule.exports = function dispatchRequest(config) {\n  throwIfCancellationRequested(config);\n\n  // Support baseURL config\n  if (config.baseURL && !isAbsoluteURL(config.url)) {\n    config.url = combineURLs(config.baseURL, config.url);\n  }\n\n  // Ensure headers exist\n  config.headers = config.headers || {};\n\n  // Transform request data\n  config.data = transformData(\n    config.data,\n    config.headers,\n    config.transformRequest\n  );\n\n  // Flatten headers\n  config.headers = utils.merge(\n    config.headers.common || {},\n    config.headers[config.method] || {},\n    config.headers || {}\n  );\n\n  utils.forEach(\n    ['delete', 'get', 'head', 'post', 'put', 'patch', 'common'],\n    function cleanHeaderConfig(method) {\n      delete config.headers[method];\n    }\n  );\n\n  var adapter = config.adapter || defaults.adapter;\n\n  return adapter(config).then(function onAdapterResolution(response) {\n    throwIfCancellationRequested(config);\n\n    // Transform response data\n    response.data = transformData(\n      response.data,\n      response.headers,\n      config.transformResponse\n    );\n\n    return response;\n  }, function onAdapterRejection(reason) {\n    if (!isCancel(reason)) {\n      throwIfCancellationRequested(config);\n\n      // Transform response data\n      if (reason && reason.response) {\n        reason.response.data = transformData(\n          reason.response.data,\n          reason.response.headers,\n          config.transformResponse\n        );\n      }\n    }\n\n    return Promise.reject(reason);\n  });\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/core/dispatchRequest.js?");

/***/ }),

/***/ "./node_modules/axios/lib/core/enhanceError.js":
/*!*****************************************************!*\
  !*** ./node_modules/axios/lib/core/enhanceError.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\n/**\n * Update an Error with the specified config, error code, and response.\n *\n * @param {Error} error The error to update.\n * @param {Object} config The config.\n * @param {string} [code] The error code (for example, 'ECONNABORTED').\n * @param {Object} [request] The request.\n * @param {Object} [response] The response.\n * @returns {Error} The error.\n */\nmodule.exports = function enhanceError(error, config, code, request, response) {\n  error.config = config;\n  if (code) {\n    error.code = code;\n  }\n  error.request = request;\n  error.response = response;\n  return error;\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/core/enhanceError.js?");

/***/ }),

/***/ "./node_modules/axios/lib/core/settle.js":
/*!***********************************************!*\
  !*** ./node_modules/axios/lib/core/settle.js ***!
  \***********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar createError = __webpack_require__(/*! ./createError */ \"./node_modules/axios/lib/core/createError.js\");\n\n/**\n * Resolve or reject a Promise based on response status.\n *\n * @param {Function} resolve A function that resolves the promise.\n * @param {Function} reject A function that rejects the promise.\n * @param {object} response The response.\n */\nmodule.exports = function settle(resolve, reject, response) {\n  var validateStatus = response.config.validateStatus;\n  // Note: status is not exposed by XDomainRequest\n  if (!response.status || !validateStatus || validateStatus(response.status)) {\n    resolve(response);\n  } else {\n    reject(createError(\n      'Request failed with status code ' + response.status,\n      response.config,\n      null,\n      response.request,\n      response\n    ));\n  }\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/core/settle.js?");

/***/ }),

/***/ "./node_modules/axios/lib/core/transformData.js":
/*!******************************************************!*\
  !*** ./node_modules/axios/lib/core/transformData.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ./../utils */ \"./node_modules/axios/lib/utils.js\");\n\n/**\n * Transform the data for a request or a response\n *\n * @param {Object|String} data The data to be transformed\n * @param {Array} headers The headers for the request or response\n * @param {Array|Function} fns A single function or Array of functions\n * @returns {*} The resulting transformed data\n */\nmodule.exports = function transformData(data, headers, fns) {\n  /*eslint no-param-reassign:0*/\n  utils.forEach(fns, function transform(fn) {\n    data = fn(data, headers);\n  });\n\n  return data;\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/core/transformData.js?");

/***/ }),

/***/ "./node_modules/axios/lib/defaults.js":
/*!********************************************!*\
  !*** ./node_modules/axios/lib/defaults.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("/* WEBPACK VAR INJECTION */(function(process) {\n\nvar utils = __webpack_require__(/*! ./utils */ \"./node_modules/axios/lib/utils.js\");\nvar normalizeHeaderName = __webpack_require__(/*! ./helpers/normalizeHeaderName */ \"./node_modules/axios/lib/helpers/normalizeHeaderName.js\");\n\nvar DEFAULT_CONTENT_TYPE = {\n  'Content-Type': 'application/x-www-form-urlencoded'\n};\n\nfunction setContentTypeIfUnset(headers, value) {\n  if (!utils.isUndefined(headers) && utils.isUndefined(headers['Content-Type'])) {\n    headers['Content-Type'] = value;\n  }\n}\n\nfunction getDefaultAdapter() {\n  var adapter;\n  if (typeof XMLHttpRequest !== 'undefined') {\n    // For browsers use XHR adapter\n    adapter = __webpack_require__(/*! ./adapters/xhr */ \"./node_modules/axios/lib/adapters/xhr.js\");\n  } else if (typeof process !== 'undefined') {\n    // For node use HTTP adapter\n    adapter = __webpack_require__(/*! ./adapters/http */ \"./node_modules/axios/lib/adapters/xhr.js\");\n  }\n  return adapter;\n}\n\nvar defaults = {\n  adapter: getDefaultAdapter(),\n\n  transformRequest: [function transformRequest(data, headers) {\n    normalizeHeaderName(headers, 'Content-Type');\n    if (utils.isFormData(data) ||\n      utils.isArrayBuffer(data) ||\n      utils.isBuffer(data) ||\n      utils.isStream(data) ||\n      utils.isFile(data) ||\n      utils.isBlob(data)\n    ) {\n      return data;\n    }\n    if (utils.isArrayBufferView(data)) {\n      return data.buffer;\n    }\n    if (utils.isURLSearchParams(data)) {\n      setContentTypeIfUnset(headers, 'application/x-www-form-urlencoded;charset=utf-8');\n      return data.toString();\n    }\n    if (utils.isObject(data)) {\n      setContentTypeIfUnset(headers, 'application/json;charset=utf-8');\n      return JSON.stringify(data);\n    }\n    return data;\n  }],\n\n  transformResponse: [function transformResponse(data) {\n    /*eslint no-param-reassign:0*/\n    if (typeof data === 'string') {\n      try {\n        data = JSON.parse(data);\n      } catch (e) { /* Ignore */ }\n    }\n    return data;\n  }],\n\n  /**\n   * A timeout in milliseconds to abort a request. If set to 0 (default) a\n   * timeout is not created.\n   */\n  timeout: 0,\n\n  xsrfCookieName: 'XSRF-TOKEN',\n  xsrfHeaderName: 'X-XSRF-TOKEN',\n\n  maxContentLength: -1,\n\n  validateStatus: function validateStatus(status) {\n    return status >= 200 && status < 300;\n  }\n};\n\ndefaults.headers = {\n  common: {\n    'Accept': 'application/json, text/plain, */*'\n  }\n};\n\nutils.forEach(['delete', 'get', 'head'], function forEachMethodNoData(method) {\n  defaults.headers[method] = {};\n});\n\nutils.forEach(['post', 'put', 'patch'], function forEachMethodWithData(method) {\n  defaults.headers[method] = utils.merge(DEFAULT_CONTENT_TYPE);\n});\n\nmodule.exports = defaults;\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../process/browser.js */ \"./node_modules/process/browser.js\")))\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/defaults.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/bind.js":
/*!************************************************!*\
  !*** ./node_modules/axios/lib/helpers/bind.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nmodule.exports = function bind(fn, thisArg) {\n  return function wrap() {\n    var args = new Array(arguments.length);\n    for (var i = 0; i < args.length; i++) {\n      args[i] = arguments[i];\n    }\n    return fn.apply(thisArg, args);\n  };\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/bind.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/btoa.js":
/*!************************************************!*\
  !*** ./node_modules/axios/lib/helpers/btoa.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\n// btoa polyfill for IE<10 courtesy https://github.com/davidchambers/Base64.js\n\nvar chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=';\n\nfunction E() {\n  this.message = 'String contains an invalid character';\n}\nE.prototype = new Error;\nE.prototype.code = 5;\nE.prototype.name = 'InvalidCharacterError';\n\nfunction btoa(input) {\n  var str = String(input);\n  var output = '';\n  for (\n    // initialize result and counter\n    var block, charCode, idx = 0, map = chars;\n    // if the next str index does not exist:\n    //   change the mapping table to \"=\"\n    //   check if d has no fractional digits\n    str.charAt(idx | 0) || (map = '=', idx % 1);\n    // \"8 - idx % 1 * 8\" generates the sequence 2, 4, 6, 8\n    output += map.charAt(63 & block >> 8 - idx % 1 * 8)\n  ) {\n    charCode = str.charCodeAt(idx += 3 / 4);\n    if (charCode > 0xFF) {\n      throw new E();\n    }\n    block = block << 8 | charCode;\n  }\n  return output;\n}\n\nmodule.exports = btoa;\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/btoa.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/buildURL.js":
/*!****************************************************!*\
  !*** ./node_modules/axios/lib/helpers/buildURL.js ***!
  \****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ./../utils */ \"./node_modules/axios/lib/utils.js\");\n\nfunction encode(val) {\n  return encodeURIComponent(val).\n    replace(/%40/gi, '@').\n    replace(/%3A/gi, ':').\n    replace(/%24/g, '$').\n    replace(/%2C/gi, ',').\n    replace(/%20/g, '+').\n    replace(/%5B/gi, '[').\n    replace(/%5D/gi, ']');\n}\n\n/**\n * Build a URL by appending params to the end\n *\n * @param {string} url The base of the url (e.g., http://www.google.com)\n * @param {object} [params] The params to be appended\n * @returns {string} The formatted url\n */\nmodule.exports = function buildURL(url, params, paramsSerializer) {\n  /*eslint no-param-reassign:0*/\n  if (!params) {\n    return url;\n  }\n\n  var serializedParams;\n  if (paramsSerializer) {\n    serializedParams = paramsSerializer(params);\n  } else if (utils.isURLSearchParams(params)) {\n    serializedParams = params.toString();\n  } else {\n    var parts = [];\n\n    utils.forEach(params, function serialize(val, key) {\n      if (val === null || typeof val === 'undefined') {\n        return;\n      }\n\n      if (utils.isArray(val)) {\n        key = key + '[]';\n      } else {\n        val = [val];\n      }\n\n      utils.forEach(val, function parseValue(v) {\n        if (utils.isDate(v)) {\n          v = v.toISOString();\n        } else if (utils.isObject(v)) {\n          v = JSON.stringify(v);\n        }\n        parts.push(encode(key) + '=' + encode(v));\n      });\n    });\n\n    serializedParams = parts.join('&');\n  }\n\n  if (serializedParams) {\n    url += (url.indexOf('?') === -1 ? '?' : '&') + serializedParams;\n  }\n\n  return url;\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/buildURL.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/combineURLs.js":
/*!*******************************************************!*\
  !*** ./node_modules/axios/lib/helpers/combineURLs.js ***!
  \*******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\n/**\n * Creates a new URL by combining the specified URLs\n *\n * @param {string} baseURL The base URL\n * @param {string} relativeURL The relative URL\n * @returns {string} The combined URL\n */\nmodule.exports = function combineURLs(baseURL, relativeURL) {\n  return relativeURL\n    ? baseURL.replace(/\\/+$/, '') + '/' + relativeURL.replace(/^\\/+/, '')\n    : baseURL;\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/combineURLs.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/cookies.js":
/*!***************************************************!*\
  !*** ./node_modules/axios/lib/helpers/cookies.js ***!
  \***************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ./../utils */ \"./node_modules/axios/lib/utils.js\");\n\nmodule.exports = (\n  utils.isStandardBrowserEnv() ?\n\n  // Standard browser envs support document.cookie\n  (function standardBrowserEnv() {\n    return {\n      write: function write(name, value, expires, path, domain, secure) {\n        var cookie = [];\n        cookie.push(name + '=' + encodeURIComponent(value));\n\n        if (utils.isNumber(expires)) {\n          cookie.push('expires=' + new Date(expires).toGMTString());\n        }\n\n        if (utils.isString(path)) {\n          cookie.push('path=' + path);\n        }\n\n        if (utils.isString(domain)) {\n          cookie.push('domain=' + domain);\n        }\n\n        if (secure === true) {\n          cookie.push('secure');\n        }\n\n        document.cookie = cookie.join('; ');\n      },\n\n      read: function read(name) {\n        var match = document.cookie.match(new RegExp('(^|;\\\\s*)(' + name + ')=([^;]*)'));\n        return (match ? decodeURIComponent(match[3]) : null);\n      },\n\n      remove: function remove(name) {\n        this.write(name, '', Date.now() - 86400000);\n      }\n    };\n  })() :\n\n  // Non standard browser env (web workers, react-native) lack needed support.\n  (function nonStandardBrowserEnv() {\n    return {\n      write: function write() {},\n      read: function read() { return null; },\n      remove: function remove() {}\n    };\n  })()\n);\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/cookies.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/isAbsoluteURL.js":
/*!*********************************************************!*\
  !*** ./node_modules/axios/lib/helpers/isAbsoluteURL.js ***!
  \*********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\n/**\n * Determines whether the specified URL is absolute\n *\n * @param {string} url The URL to test\n * @returns {boolean} True if the specified URL is absolute, otherwise false\n */\nmodule.exports = function isAbsoluteURL(url) {\n  // A URL is considered absolute if it begins with \"<scheme>://\" or \"//\" (protocol-relative URL).\n  // RFC 3986 defines scheme name as a sequence of characters beginning with a letter and followed\n  // by any combination of letters, digits, plus, period, or hyphen.\n  return /^([a-z][a-z\\d\\+\\-\\.]*:)?\\/\\//i.test(url);\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/isAbsoluteURL.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/isURLSameOrigin.js":
/*!***********************************************************!*\
  !*** ./node_modules/axios/lib/helpers/isURLSameOrigin.js ***!
  \***********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ./../utils */ \"./node_modules/axios/lib/utils.js\");\n\nmodule.exports = (\n  utils.isStandardBrowserEnv() ?\n\n  // Standard browser envs have full support of the APIs needed to test\n  // whether the request URL is of the same origin as current location.\n  (function standardBrowserEnv() {\n    var msie = /(msie|trident)/i.test(navigator.userAgent);\n    var urlParsingNode = document.createElement('a');\n    var originURL;\n\n    /**\n    * Parse a URL to discover it's components\n    *\n    * @param {String} url The URL to be parsed\n    * @returns {Object}\n    */\n    function resolveURL(url) {\n      var href = url;\n\n      if (msie) {\n        // IE needs attribute set twice to normalize properties\n        urlParsingNode.setAttribute('href', href);\n        href = urlParsingNode.href;\n      }\n\n      urlParsingNode.setAttribute('href', href);\n\n      // urlParsingNode provides the UrlUtils interface - http://url.spec.whatwg.org/#urlutils\n      return {\n        href: urlParsingNode.href,\n        protocol: urlParsingNode.protocol ? urlParsingNode.protocol.replace(/:$/, '') : '',\n        host: urlParsingNode.host,\n        search: urlParsingNode.search ? urlParsingNode.search.replace(/^\\?/, '') : '',\n        hash: urlParsingNode.hash ? urlParsingNode.hash.replace(/^#/, '') : '',\n        hostname: urlParsingNode.hostname,\n        port: urlParsingNode.port,\n        pathname: (urlParsingNode.pathname.charAt(0) === '/') ?\n                  urlParsingNode.pathname :\n                  '/' + urlParsingNode.pathname\n      };\n    }\n\n    originURL = resolveURL(window.location.href);\n\n    /**\n    * Determine if a URL shares the same origin as the current location\n    *\n    * @param {String} requestURL The URL to test\n    * @returns {boolean} True if URL shares the same origin, otherwise false\n    */\n    return function isURLSameOrigin(requestURL) {\n      var parsed = (utils.isString(requestURL)) ? resolveURL(requestURL) : requestURL;\n      return (parsed.protocol === originURL.protocol &&\n            parsed.host === originURL.host);\n    };\n  })() :\n\n  // Non standard browser envs (web workers, react-native) lack needed support.\n  (function nonStandardBrowserEnv() {\n    return function isURLSameOrigin() {\n      return true;\n    };\n  })()\n);\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/isURLSameOrigin.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/normalizeHeaderName.js":
/*!***************************************************************!*\
  !*** ./node_modules/axios/lib/helpers/normalizeHeaderName.js ***!
  \***************************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ../utils */ \"./node_modules/axios/lib/utils.js\");\n\nmodule.exports = function normalizeHeaderName(headers, normalizedName) {\n  utils.forEach(headers, function processHeader(value, name) {\n    if (name !== normalizedName && name.toUpperCase() === normalizedName.toUpperCase()) {\n      headers[normalizedName] = value;\n      delete headers[name];\n    }\n  });\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/normalizeHeaderName.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/parseHeaders.js":
/*!********************************************************!*\
  !*** ./node_modules/axios/lib/helpers/parseHeaders.js ***!
  \********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar utils = __webpack_require__(/*! ./../utils */ \"./node_modules/axios/lib/utils.js\");\n\n// Headers whose duplicates are ignored by node\n// c.f. https://nodejs.org/api/http.html#http_message_headers\nvar ignoreDuplicateOf = [\n  'age', 'authorization', 'content-length', 'content-type', 'etag',\n  'expires', 'from', 'host', 'if-modified-since', 'if-unmodified-since',\n  'last-modified', 'location', 'max-forwards', 'proxy-authorization',\n  'referer', 'retry-after', 'user-agent'\n];\n\n/**\n * Parse headers into an object\n *\n * ```\n * Date: Wed, 27 Aug 2014 08:58:49 GMT\n * Content-Type: application/json\n * Connection: keep-alive\n * Transfer-Encoding: chunked\n * ```\n *\n * @param {String} headers Headers needing to be parsed\n * @returns {Object} Headers parsed into an object\n */\nmodule.exports = function parseHeaders(headers) {\n  var parsed = {};\n  var key;\n  var val;\n  var i;\n\n  if (!headers) { return parsed; }\n\n  utils.forEach(headers.split('\\n'), function parser(line) {\n    i = line.indexOf(':');\n    key = utils.trim(line.substr(0, i)).toLowerCase();\n    val = utils.trim(line.substr(i + 1));\n\n    if (key) {\n      if (parsed[key] && ignoreDuplicateOf.indexOf(key) >= 0) {\n        return;\n      }\n      if (key === 'set-cookie') {\n        parsed[key] = (parsed[key] ? parsed[key] : []).concat([val]);\n      } else {\n        parsed[key] = parsed[key] ? parsed[key] + ', ' + val : val;\n      }\n    }\n  });\n\n  return parsed;\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/parseHeaders.js?");

/***/ }),

/***/ "./node_modules/axios/lib/helpers/spread.js":
/*!**************************************************!*\
  !*** ./node_modules/axios/lib/helpers/spread.js ***!
  \**************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\n/**\n * Syntactic sugar for invoking a function and expanding an array for arguments.\n *\n * Common use case would be to use `Function.prototype.apply`.\n *\n *  ```js\n *  function f(x, y, z) {}\n *  var args = [1, 2, 3];\n *  f.apply(null, args);\n *  ```\n *\n * With `spread` this example can be re-written.\n *\n *  ```js\n *  spread(function(x, y, z) {})([1, 2, 3]);\n *  ```\n *\n * @param {Function} callback\n * @returns {Function}\n */\nmodule.exports = function spread(callback) {\n  return function wrap(arr) {\n    return callback.apply(null, arr);\n  };\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/helpers/spread.js?");

/***/ }),

/***/ "./node_modules/axios/lib/utils.js":
/*!*****************************************!*\
  !*** ./node_modules/axios/lib/utils.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("\n\nvar bind = __webpack_require__(/*! ./helpers/bind */ \"./node_modules/axios/lib/helpers/bind.js\");\nvar isBuffer = __webpack_require__(/*! is-buffer */ \"./node_modules/is-buffer/index.js\");\n\n/*global toString:true*/\n\n// utils is a library of generic helper functions non-specific to axios\n\nvar toString = Object.prototype.toString;\n\n/**\n * Determine if a value is an Array\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is an Array, otherwise false\n */\nfunction isArray(val) {\n  return toString.call(val) === '[object Array]';\n}\n\n/**\n * Determine if a value is an ArrayBuffer\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is an ArrayBuffer, otherwise false\n */\nfunction isArrayBuffer(val) {\n  return toString.call(val) === '[object ArrayBuffer]';\n}\n\n/**\n * Determine if a value is a FormData\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is an FormData, otherwise false\n */\nfunction isFormData(val) {\n  return (typeof FormData !== 'undefined') && (val instanceof FormData);\n}\n\n/**\n * Determine if a value is a view on an ArrayBuffer\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a view on an ArrayBuffer, otherwise false\n */\nfunction isArrayBufferView(val) {\n  var result;\n  if ((typeof ArrayBuffer !== 'undefined') && (ArrayBuffer.isView)) {\n    result = ArrayBuffer.isView(val);\n  } else {\n    result = (val) && (val.buffer) && (val.buffer instanceof ArrayBuffer);\n  }\n  return result;\n}\n\n/**\n * Determine if a value is a String\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a String, otherwise false\n */\nfunction isString(val) {\n  return typeof val === 'string';\n}\n\n/**\n * Determine if a value is a Number\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a Number, otherwise false\n */\nfunction isNumber(val) {\n  return typeof val === 'number';\n}\n\n/**\n * Determine if a value is undefined\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if the value is undefined, otherwise false\n */\nfunction isUndefined(val) {\n  return typeof val === 'undefined';\n}\n\n/**\n * Determine if a value is an Object\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is an Object, otherwise false\n */\nfunction isObject(val) {\n  return val !== null && typeof val === 'object';\n}\n\n/**\n * Determine if a value is a Date\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a Date, otherwise false\n */\nfunction isDate(val) {\n  return toString.call(val) === '[object Date]';\n}\n\n/**\n * Determine if a value is a File\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a File, otherwise false\n */\nfunction isFile(val) {\n  return toString.call(val) === '[object File]';\n}\n\n/**\n * Determine if a value is a Blob\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a Blob, otherwise false\n */\nfunction isBlob(val) {\n  return toString.call(val) === '[object Blob]';\n}\n\n/**\n * Determine if a value is a Function\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a Function, otherwise false\n */\nfunction isFunction(val) {\n  return toString.call(val) === '[object Function]';\n}\n\n/**\n * Determine if a value is a Stream\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a Stream, otherwise false\n */\nfunction isStream(val) {\n  return isObject(val) && isFunction(val.pipe);\n}\n\n/**\n * Determine if a value is a URLSearchParams object\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a URLSearchParams object, otherwise false\n */\nfunction isURLSearchParams(val) {\n  return typeof URLSearchParams !== 'undefined' && val instanceof URLSearchParams;\n}\n\n/**\n * Trim excess whitespace off the beginning and end of a string\n *\n * @param {String} str The String to trim\n * @returns {String} The String freed of excess whitespace\n */\nfunction trim(str) {\n  return str.replace(/^\\s*/, '').replace(/\\s*$/, '');\n}\n\n/**\n * Determine if we're running in a standard browser environment\n *\n * This allows axios to run in a web worker, and react-native.\n * Both environments support XMLHttpRequest, but not fully standard globals.\n *\n * web workers:\n *  typeof window -> undefined\n *  typeof document -> undefined\n *\n * react-native:\n *  navigator.product -> 'ReactNative'\n */\nfunction isStandardBrowserEnv() {\n  if (typeof navigator !== 'undefined' && navigator.product === 'ReactNative') {\n    return false;\n  }\n  return (\n    typeof window !== 'undefined' &&\n    typeof document !== 'undefined'\n  );\n}\n\n/**\n * Iterate over an Array or an Object invoking a function for each item.\n *\n * If `obj` is an Array callback will be called passing\n * the value, index, and complete array for each item.\n *\n * If 'obj' is an Object callback will be called passing\n * the value, key, and complete object for each property.\n *\n * @param {Object|Array} obj The object to iterate\n * @param {Function} fn The callback to invoke for each item\n */\nfunction forEach(obj, fn) {\n  // Don't bother if no value provided\n  if (obj === null || typeof obj === 'undefined') {\n    return;\n  }\n\n  // Force an array if not already something iterable\n  if (typeof obj !== 'object') {\n    /*eslint no-param-reassign:0*/\n    obj = [obj];\n  }\n\n  if (isArray(obj)) {\n    // Iterate over array values\n    for (var i = 0, l = obj.length; i < l; i++) {\n      fn.call(null, obj[i], i, obj);\n    }\n  } else {\n    // Iterate over object keys\n    for (var key in obj) {\n      if (Object.prototype.hasOwnProperty.call(obj, key)) {\n        fn.call(null, obj[key], key, obj);\n      }\n    }\n  }\n}\n\n/**\n * Accepts varargs expecting each argument to be an object, then\n * immutably merges the properties of each object and returns result.\n *\n * When multiple objects contain the same key the later object in\n * the arguments list will take precedence.\n *\n * Example:\n *\n * ```js\n * var result = merge({foo: 123}, {foo: 456});\n * console.log(result.foo); // outputs 456\n * ```\n *\n * @param {Object} obj1 Object to merge\n * @returns {Object} Result of all merge properties\n */\nfunction merge(/* obj1, obj2, obj3, ... */) {\n  var result = {};\n  function assignValue(val, key) {\n    if (typeof result[key] === 'object' && typeof val === 'object') {\n      result[key] = merge(result[key], val);\n    } else {\n      result[key] = val;\n    }\n  }\n\n  for (var i = 0, l = arguments.length; i < l; i++) {\n    forEach(arguments[i], assignValue);\n  }\n  return result;\n}\n\n/**\n * Extends object a by mutably adding to it the properties of object b.\n *\n * @param {Object} a The object to be extended\n * @param {Object} b The object to copy properties from\n * @param {Object} thisArg The object to bind function to\n * @return {Object} The resulting value of object a\n */\nfunction extend(a, b, thisArg) {\n  forEach(b, function assignValue(val, key) {\n    if (thisArg && typeof val === 'function') {\n      a[key] = bind(val, thisArg);\n    } else {\n      a[key] = val;\n    }\n  });\n  return a;\n}\n\nmodule.exports = {\n  isArray: isArray,\n  isArrayBuffer: isArrayBuffer,\n  isBuffer: isBuffer,\n  isFormData: isFormData,\n  isArrayBufferView: isArrayBufferView,\n  isString: isString,\n  isNumber: isNumber,\n  isObject: isObject,\n  isUndefined: isUndefined,\n  isDate: isDate,\n  isFile: isFile,\n  isBlob: isBlob,\n  isFunction: isFunction,\n  isStream: isStream,\n  isURLSearchParams: isURLSearchParams,\n  isStandardBrowserEnv: isStandardBrowserEnv,\n  forEach: forEach,\n  merge: merge,\n  extend: extend,\n  trim: trim\n};\n\n\n//# sourceURL=webpack://Penta/./node_modules/axios/lib/utils.js?");

/***/ }),

/***/ "./node_modules/is-buffer/index.js":
/*!*****************************************!*\
  !*** ./node_modules/is-buffer/index.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

eval("/*!\n * Determine if an object is a Buffer\n *\n * @author   Feross Aboukhadijeh <https://feross.org>\n * @license  MIT\n */\n\n// The _isBuffer check is for Safari 5-7 support, because it's missing\n// Object.prototype.constructor. Remove this eventually\nmodule.exports = function (obj) {\n  return obj != null && (isBuffer(obj) || isSlowBuffer(obj) || !!obj._isBuffer)\n}\n\nfunction isBuffer (obj) {\n  return !!obj.constructor && typeof obj.constructor.isBuffer === 'function' && obj.constructor.isBuffer(obj)\n}\n\n// For Node v0.10 support. Remove this eventually.\nfunction isSlowBuffer (obj) {\n  return typeof obj.readFloatLE === 'function' && typeof obj.slice === 'function' && isBuffer(obj.slice(0, 0))\n}\n\n\n//# sourceURL=webpack://Penta/./node_modules/is-buffer/index.js?");

/***/ }),

/***/ "./node_modules/process/browser.js":
/*!*****************************************!*\
  !*** ./node_modules/process/browser.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

eval("// shim for using process in browser\nvar process = module.exports = {};\n\n// cached from whatever global is present so that test runners that stub it\n// don't break things.  But we need to wrap it in a try catch in case it is\n// wrapped in strict mode code which doesn't define any globals.  It's inside a\n// function because try/catches deoptimize in certain engines.\n\nvar cachedSetTimeout;\nvar cachedClearTimeout;\n\nfunction defaultSetTimout() {\n    throw new Error('setTimeout has not been defined');\n}\nfunction defaultClearTimeout () {\n    throw new Error('clearTimeout has not been defined');\n}\n(function () {\n    try {\n        if (typeof setTimeout === 'function') {\n            cachedSetTimeout = setTimeout;\n        } else {\n            cachedSetTimeout = defaultSetTimout;\n        }\n    } catch (e) {\n        cachedSetTimeout = defaultSetTimout;\n    }\n    try {\n        if (typeof clearTimeout === 'function') {\n            cachedClearTimeout = clearTimeout;\n        } else {\n            cachedClearTimeout = defaultClearTimeout;\n        }\n    } catch (e) {\n        cachedClearTimeout = defaultClearTimeout;\n    }\n} ())\nfunction runTimeout(fun) {\n    if (cachedSetTimeout === setTimeout) {\n        //normal enviroments in sane situations\n        return setTimeout(fun, 0);\n    }\n    // if setTimeout wasn't available but was latter defined\n    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {\n        cachedSetTimeout = setTimeout;\n        return setTimeout(fun, 0);\n    }\n    try {\n        // when when somebody has screwed with setTimeout but no I.E. maddness\n        return cachedSetTimeout(fun, 0);\n    } catch(e){\n        try {\n            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally\n            return cachedSetTimeout.call(null, fun, 0);\n        } catch(e){\n            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error\n            return cachedSetTimeout.call(this, fun, 0);\n        }\n    }\n\n\n}\nfunction runClearTimeout(marker) {\n    if (cachedClearTimeout === clearTimeout) {\n        //normal enviroments in sane situations\n        return clearTimeout(marker);\n    }\n    // if clearTimeout wasn't available but was latter defined\n    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {\n        cachedClearTimeout = clearTimeout;\n        return clearTimeout(marker);\n    }\n    try {\n        // when when somebody has screwed with setTimeout but no I.E. maddness\n        return cachedClearTimeout(marker);\n    } catch (e){\n        try {\n            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally\n            return cachedClearTimeout.call(null, marker);\n        } catch (e){\n            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.\n            // Some versions of I.E. have different rules for clearTimeout vs setTimeout\n            return cachedClearTimeout.call(this, marker);\n        }\n    }\n\n\n\n}\nvar queue = [];\nvar draining = false;\nvar currentQueue;\nvar queueIndex = -1;\n\nfunction cleanUpNextTick() {\n    if (!draining || !currentQueue) {\n        return;\n    }\n    draining = false;\n    if (currentQueue.length) {\n        queue = currentQueue.concat(queue);\n    } else {\n        queueIndex = -1;\n    }\n    if (queue.length) {\n        drainQueue();\n    }\n}\n\nfunction drainQueue() {\n    if (draining) {\n        return;\n    }\n    var timeout = runTimeout(cleanUpNextTick);\n    draining = true;\n\n    var len = queue.length;\n    while(len) {\n        currentQueue = queue;\n        queue = [];\n        while (++queueIndex < len) {\n            if (currentQueue) {\n                currentQueue[queueIndex].run();\n            }\n        }\n        queueIndex = -1;\n        len = queue.length;\n    }\n    currentQueue = null;\n    draining = false;\n    runClearTimeout(timeout);\n}\n\nprocess.nextTick = function (fun) {\n    var args = new Array(arguments.length - 1);\n    if (arguments.length > 1) {\n        for (var i = 1; i < arguments.length; i++) {\n            args[i - 1] = arguments[i];\n        }\n    }\n    queue.push(new Item(fun, args));\n    if (queue.length === 1 && !draining) {\n        runTimeout(drainQueue);\n    }\n};\n\n// v8 likes predictible objects\nfunction Item(fun, array) {\n    this.fun = fun;\n    this.array = array;\n}\nItem.prototype.run = function () {\n    this.fun.apply(null, this.array);\n};\nprocess.title = 'browser';\nprocess.browser = true;\nprocess.env = {};\nprocess.argv = [];\nprocess.version = ''; // empty string to avoid regexp issues\nprocess.versions = {};\n\nfunction noop() {}\n\nprocess.on = noop;\nprocess.addListener = noop;\nprocess.once = noop;\nprocess.off = noop;\nprocess.removeListener = noop;\nprocess.removeAllListeners = noop;\nprocess.emit = noop;\nprocess.prependListener = noop;\nprocess.prependOnceListener = noop;\n\nprocess.listeners = function (name) { return [] }\n\nprocess.binding = function (name) {\n    throw new Error('process.binding is not supported');\n};\n\nprocess.cwd = function () { return '/' };\nprocess.chdir = function (dir) {\n    throw new Error('process.chdir is not supported');\n};\nprocess.umask = function() { return 0; };\n\n\n//# sourceURL=webpack://Penta/./node_modules/process/browser.js?");

/***/ }),

/***/ "./node_modules/utf8/utf8.js":
/*!***********************************!*\
  !*** ./node_modules/utf8/utf8.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("/*! https://mths.be/utf8js v3.0.0 by @mathias */\n;(function(root) {\n\n\tvar stringFromCharCode = String.fromCharCode;\n\n\t// Taken from https://mths.be/punycode\n\tfunction ucs2decode(string) {\n\t\tvar output = [];\n\t\tvar counter = 0;\n\t\tvar length = string.length;\n\t\tvar value;\n\t\tvar extra;\n\t\twhile (counter < length) {\n\t\t\tvalue = string.charCodeAt(counter++);\n\t\t\tif (value >= 0xD800 && value <= 0xDBFF && counter < length) {\n\t\t\t\t// high surrogate, and there is a next character\n\t\t\t\textra = string.charCodeAt(counter++);\n\t\t\t\tif ((extra & 0xFC00) == 0xDC00) { // low surrogate\n\t\t\t\t\toutput.push(((value & 0x3FF) << 10) + (extra & 0x3FF) + 0x10000);\n\t\t\t\t} else {\n\t\t\t\t\t// unmatched surrogate; only append this code unit, in case the next\n\t\t\t\t\t// code unit is the high surrogate of a surrogate pair\n\t\t\t\t\toutput.push(value);\n\t\t\t\t\tcounter--;\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\toutput.push(value);\n\t\t\t}\n\t\t}\n\t\treturn output;\n\t}\n\n\t// Taken from https://mths.be/punycode\n\tfunction ucs2encode(array) {\n\t\tvar length = array.length;\n\t\tvar index = -1;\n\t\tvar value;\n\t\tvar output = '';\n\t\twhile (++index < length) {\n\t\t\tvalue = array[index];\n\t\t\tif (value > 0xFFFF) {\n\t\t\t\tvalue -= 0x10000;\n\t\t\t\toutput += stringFromCharCode(value >>> 10 & 0x3FF | 0xD800);\n\t\t\t\tvalue = 0xDC00 | value & 0x3FF;\n\t\t\t}\n\t\t\toutput += stringFromCharCode(value);\n\t\t}\n\t\treturn output;\n\t}\n\n\tfunction checkScalarValue(codePoint) {\n\t\tif (codePoint >= 0xD800 && codePoint <= 0xDFFF) {\n\t\t\tthrow Error(\n\t\t\t\t'Lone surrogate U+' + codePoint.toString(16).toUpperCase() +\n\t\t\t\t' is not a scalar value'\n\t\t\t);\n\t\t}\n\t}\n\t/*--------------------------------------------------------------------------*/\n\n\tfunction createByte(codePoint, shift) {\n\t\treturn stringFromCharCode(((codePoint >> shift) & 0x3F) | 0x80);\n\t}\n\n\tfunction encodeCodePoint(codePoint) {\n\t\tif ((codePoint & 0xFFFFFF80) == 0) { // 1-byte sequence\n\t\t\treturn stringFromCharCode(codePoint);\n\t\t}\n\t\tvar symbol = '';\n\t\tif ((codePoint & 0xFFFFF800) == 0) { // 2-byte sequence\n\t\t\tsymbol = stringFromCharCode(((codePoint >> 6) & 0x1F) | 0xC0);\n\t\t}\n\t\telse if ((codePoint & 0xFFFF0000) == 0) { // 3-byte sequence\n\t\t\tcheckScalarValue(codePoint);\n\t\t\tsymbol = stringFromCharCode(((codePoint >> 12) & 0x0F) | 0xE0);\n\t\t\tsymbol += createByte(codePoint, 6);\n\t\t}\n\t\telse if ((codePoint & 0xFFE00000) == 0) { // 4-byte sequence\n\t\t\tsymbol = stringFromCharCode(((codePoint >> 18) & 0x07) | 0xF0);\n\t\t\tsymbol += createByte(codePoint, 12);\n\t\t\tsymbol += createByte(codePoint, 6);\n\t\t}\n\t\tsymbol += stringFromCharCode((codePoint & 0x3F) | 0x80);\n\t\treturn symbol;\n\t}\n\n\tfunction utf8encode(string) {\n\t\tvar codePoints = ucs2decode(string);\n\t\tvar length = codePoints.length;\n\t\tvar index = -1;\n\t\tvar codePoint;\n\t\tvar byteString = '';\n\t\twhile (++index < length) {\n\t\t\tcodePoint = codePoints[index];\n\t\t\tbyteString += encodeCodePoint(codePoint);\n\t\t}\n\t\treturn byteString;\n\t}\n\n\t/*--------------------------------------------------------------------------*/\n\n\tfunction readContinuationByte() {\n\t\tif (byteIndex >= byteCount) {\n\t\t\tthrow Error('Invalid byte index');\n\t\t}\n\n\t\tvar continuationByte = byteArray[byteIndex] & 0xFF;\n\t\tbyteIndex++;\n\n\t\tif ((continuationByte & 0xC0) == 0x80) {\n\t\t\treturn continuationByte & 0x3F;\n\t\t}\n\n\t\t// If we end up here, it’s not a continuation byte\n\t\tthrow Error('Invalid continuation byte');\n\t}\n\n\tfunction decodeSymbol() {\n\t\tvar byte1;\n\t\tvar byte2;\n\t\tvar byte3;\n\t\tvar byte4;\n\t\tvar codePoint;\n\n\t\tif (byteIndex > byteCount) {\n\t\t\tthrow Error('Invalid byte index');\n\t\t}\n\n\t\tif (byteIndex == byteCount) {\n\t\t\treturn false;\n\t\t}\n\n\t\t// Read first byte\n\t\tbyte1 = byteArray[byteIndex] & 0xFF;\n\t\tbyteIndex++;\n\n\t\t// 1-byte sequence (no continuation bytes)\n\t\tif ((byte1 & 0x80) == 0) {\n\t\t\treturn byte1;\n\t\t}\n\n\t\t// 2-byte sequence\n\t\tif ((byte1 & 0xE0) == 0xC0) {\n\t\t\tbyte2 = readContinuationByte();\n\t\t\tcodePoint = ((byte1 & 0x1F) << 6) | byte2;\n\t\t\tif (codePoint >= 0x80) {\n\t\t\t\treturn codePoint;\n\t\t\t} else {\n\t\t\t\tthrow Error('Invalid continuation byte');\n\t\t\t}\n\t\t}\n\n\t\t// 3-byte sequence (may include unpaired surrogates)\n\t\tif ((byte1 & 0xF0) == 0xE0) {\n\t\t\tbyte2 = readContinuationByte();\n\t\t\tbyte3 = readContinuationByte();\n\t\t\tcodePoint = ((byte1 & 0x0F) << 12) | (byte2 << 6) | byte3;\n\t\t\tif (codePoint >= 0x0800) {\n\t\t\t\tcheckScalarValue(codePoint);\n\t\t\t\treturn codePoint;\n\t\t\t} else {\n\t\t\t\tthrow Error('Invalid continuation byte');\n\t\t\t}\n\t\t}\n\n\t\t// 4-byte sequence\n\t\tif ((byte1 & 0xF8) == 0xF0) {\n\t\t\tbyte2 = readContinuationByte();\n\t\t\tbyte3 = readContinuationByte();\n\t\t\tbyte4 = readContinuationByte();\n\t\t\tcodePoint = ((byte1 & 0x07) << 0x12) | (byte2 << 0x0C) |\n\t\t\t\t(byte3 << 0x06) | byte4;\n\t\t\tif (codePoint >= 0x010000 && codePoint <= 0x10FFFF) {\n\t\t\t\treturn codePoint;\n\t\t\t}\n\t\t}\n\n\t\tthrow Error('Invalid UTF-8 detected');\n\t}\n\n\tvar byteArray;\n\tvar byteCount;\n\tvar byteIndex;\n\tfunction utf8decode(byteString) {\n\t\tbyteArray = ucs2decode(byteString);\n\t\tbyteCount = byteArray.length;\n\t\tbyteIndex = 0;\n\t\tvar codePoints = [];\n\t\tvar tmp;\n\t\twhile ((tmp = decodeSymbol()) !== false) {\n\t\t\tcodePoints.push(tmp);\n\t\t}\n\t\treturn ucs2encode(codePoints);\n\t}\n\n\t/*--------------------------------------------------------------------------*/\n\n\troot.version = '3.0.0';\n\troot.encode = utf8encode;\n\troot.decode = utf8decode;\n\n}( false ? undefined : exports));\n\n\n//# sourceURL=webpack://Penta/./node_modules/utf8/utf8.js?");

/***/ }),

/***/ "./node_modules/webpack/buildin/amd-options.js":
/*!****************************************!*\
  !*** (webpack)/buildin/amd-options.js ***!
  \****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

eval("/* WEBPACK VAR INJECTION */(function(__webpack_amd_options__) {/* globals __webpack_amd_options__ */\nmodule.exports = __webpack_amd_options__;\n\n/* WEBPACK VAR INJECTION */}.call(this, {}))\n\n//# sourceURL=webpack://Penta/(webpack)/buildin/amd-options.js?");

/***/ })

/******/ });
});